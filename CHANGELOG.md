# Changelog

Every released version of d3text, newest first, generated by
`pdm run changelog` from the commit history. Do not edit by hand.

## 0.14.1 — 2026-09-24

### Fixed

- Widen strain-designation matching past connectors and gaps (`f75596b`)
- Raise on a non-finite selection score (`190f0c0`)
- Refuse to append tune results under a mismatched CSV header (`4bcecdb`)
- Step ReduceLROnPlateau on the selection score, not loss (`d815df0`)
- Don't record an fp16 step the GradScaler skipped (`3c91e0c`)
- Pick the same duplicate-pubmed_id row's text as training does (`eeeb969`)

### Performance

- Drop the validation loss pass (`ce7f609`)

### Changed

- Type fix_keys_hook's state dict and key lists precisely (`62e815e`)

### Internal

- Pin document_fields' offset and straddle invariants (`00b426f`)
- Document BatchUpdate.zero_grad and __call__ (`063f087`)

## 0.14.0 — 2026-09-24

### Added

- Add class_head_lr, a separate learning rate for the class head (`354c82d`)
- Print each epoch's selection score and its factors (`417867e`)

### Fixed

- Type dataset_metrics entities by schema, not a class_map guess (`1ff79be`)
- Default precompute-embeddings' --stream_batch to STREAM_BATCH (`c0c4429`)

### Changed

- Drop corpus's unreachable ReDoS-timeout row skip (`f0db823`)

### Internal

- Say dataset/classes excludes the OOS column (`6fe4e60`)

## 0.13.0 — 2026-09-24

### Added

- Widen the culture-collection acronym list from cafi (`bc64e2c`)

### Fixed

- Widen an unclaimed accession onto its thousands tail (`83f68c3`)
- Drop every _neg_inf key in fix_keys_hook, not just the root (`0431e2a`)
- Select the best epoch by a validation metric, not loss (`4702b90`)

### Performance

- Bisect the mention overlap check for unclaimed accessions (`3787b04`)

### Internal

- Say exact match opens the designation route (`3c1a015`)
- Drop tool-specific tags from comments (`bb50778`)
- Cite a real SF collision in the accession exclusions (`75658dc`)

## 0.12.4 — 2026-09-24

### Fixed

- Mark strain designations and accessions IGNORE, not OUTSIDE (`c3bec73`)
- Run the tagger under the model's autocast when reading spans (`85f3c3a`)
- Accept a layer-boundary store with every encoder layer unfrozen (`1363827`)

## 0.12.3 — 2026-09-24

### Fixed

- Make compiled training draw the same dropout masks as eager (`67e033a`)

### Internal

- Stub both CUDA branches of environment_tags by value (`724cf25`)
- Prove _git anchors on the package dir, not the cwd (`7838263`)
- Docstring the remaining taxonomy tests, fix a misnamed one (`3ed6681`)
- Drop test_tracking.py's weaker copy of a base.py test (`99e6a75`)

## 0.12.2 — 2026-09-24

### Fixed

- Summarize missing token labels once per pass (`c7873e2`)
- Refuse a genus abbreviation the caller cannot vouch for (`13fc21b`)

### Performance

- Compile the trainable top encoder layers, not the model (`461183e`)

### Internal

- Isolate config.py rejection tests from the ETE store default (`a2b2b14`)
- Pin that a changed decorator moves a rule's fingerprint (`e9b5b1b`)
- Load a deadline=None Hypothesis profile for the whole suite (`666c2a2`)
- Stub the corpus dir for the corpus-defaulting tests (`0fc02f9`)
- Drop an LPSN lookup that ran at collection for no reason (`09c1c7b`)

## 0.12.1 — 2026-09-23

### Performance

- Stop dynamo tracing spans_from_codes under compile (`ac26ad3`)

## 0.12.0 — 2026-09-23

### Breaking

- Make torch.compile opt-in via D3TEXT_COMPILE (`c4a186d`)

  Compiling has not paid for this model: with the trunk training, the
  first epoch ran about six times slower than eager for a sub-1% gain per
  later epoch. Training now runs eager unless D3TEXT_COMPILE is set; the
  D3TEXT_DISABLE_COMPILE switch is gone.


### Fixed

- Refuse a fuzzy near-miss on a dropped bacterium synonym too (`c9e2d93`)

## 0.11.0 — 2026-09-23

### Added

- Print epoch pass times (`2dbb63b`)
- Map infer records onto annotation-hub's save shape (`0cd468f`)
- Log each split's real and synthetic document counts (`14bd34a`)

### Fixed

- Refuse a dataset whose store never encoded a whole source (`4e8b062`)
- Give sync_doc_db's strain adapter the sink apiadapters requires (`94de25f`)
- Hoist the relation-aligner's and accession-respeller's closures (`247bcb8`)
- Open a tuning trial's run before its setup can fail (`0f62ccb`)

### Performance

- Add GB10 training profile harness (`c0395db`)
- Stop early after two epochs without improvement (`3f9252a`)

### Internal

- Restore the plain base-model doubles in test_base (`499d817`)

## 0.10.3 — 2026-09-22

### Fixed

- Remove closure leaking cached trunk prefixes (`1224ba6`)

### Performance

- Batch detected argument pooling (`cbcfc9a`)
- Sample unique tuning configs lazily (`62f62b4`)

## 0.10.2 — 2026-09-22

### Fixed

- Type populate_layer_boundary_store's datasets as Path, not str (`44332b6`)

### Performance

- Forward unpadded windows without an attention mask (`e0fc323`)

## 0.10.1 — 2026-09-22

### Fixed

- Let a tuning sweep survive a trial that fails (`721cd77`)
- Stop the token-label reader leaking a closure per lookup (`3f27082`)

## 0.10.0 — 2026-09-22

### Added

- Cache the frozen trunk prefix when top layers train (`91022d1`)

### Fixed

- Compose the ambiguous down-weight with balanced and focal (`442c012`)

## 0.9.0 — 2026-09-22

### Added

- Key embeddings_store paths in config.toml by base model (`15909ed`)

### Performance

- Overlap embed_document calls to hide GPU-wait behind tokenize (`b2270a2`)

## 0.8.1 — 2026-09-22

### Fixed

- Remove unwired compute_batch_true_x_pred from ETEBrendaModel (`d2297c8`)

## 0.8.0 — 2026-09-22

### Added

- Count token-label cache hits, misses and declines per pass (`c94eaad`)

## 0.7.2 — 2026-09-21

### Fixed

- Raise on a store hit too large for fp16, not inf (`4c5f4bc`)
- Pass ambiguous=None to the token loss under balancing (`32e9918`)

### Internal

- Note HDF5 file locking backs the digest cross-process guard (`ba2afca`)

## 0.7.1 — 2026-09-21

### Fixed

- Resolve the documents.json dump against BRENDA_DATA_DIR too (`c90b13e`)

## 0.7.0 — 2026-09-21

### Added

- Default precompute-token-labels' entity tables from config (`d300526`)

## 0.6.0 — 2026-09-21

### Added

- Register nlpie/compact-biobert and nlpie/tiny-biobert encodings (`6420e6a`)

### Fixed

- Stop double-logging average validation loss (`194b3af`)

### Internal

- Detect index construction without enzyme gold (`f9256db`)

## 0.5.5 — 2026-09-21

### Changed

- Stop storing the encodings' sample mapping (`ca43a7e`)

## 0.5.4 — 2026-09-21

### Fixed

- Release a tuning trial before the next one allocates (`03f9b6e`)

## 0.5.3 — 2026-09-20

### Fixed

- Encode a document listed twice in one window only once (`75967e7`)

## 0.5.2 — 2026-09-20

### Fixed

- Mint an external document's cache id from its key (`df4204e`)
- Reject a json corpus file that carries no body column (`05825a6`)

### Internal

- Sweep cli/ for the span-tagger probe instead of two functions (`289421d`)
- Replace backlog identifiers with the facts they stood for (`bc3efb2`)

## 0.5.1 — 2026-09-20

### Fixed

- Reject a cached embedding whose row count is not the document's (`e117fb4`)

### Changed

- Give the corpus streaming batch size one owner (`19c445d`)
- Give the span-tagger probe one home (`a8ac635`)

### Internal

- Drop the dec03 pooling experiment scripts (`3865949`)
- Sweep the loss-shaping knobs instead of the settled ones (`6bace95`)

## 0.5.0 — 2026-09-20

### Added

- Record what the embeddings store served a run (`a3f9787`)
- Read the corpus file list from configuration (`ebb8ec1`)
- Add a command that writes what a checkpoint predicted (`e5ccb63`)

### Performance

- Store frozen trunk linear weights in the autocast dtype (`67b46d8`)

### Changed

- Move the AMP dtype pick out of the model (`d2075ad`)

### Internal

- Add a gate that imports every console-script module (`932861e`)
- Name the noise pools in every precompute command (`81cd329`)
- Say what the store-to-span join is for (`09eb341`)

## 0.4.0 — 2026-09-20

### Performance

- Keep the gold-pair key out of the compiled region (`8922ba5`)

### Changed

- Resolve the model classes lazily in models/__init__ (`824dd07`)

## 0.3.1 — 2026-09-20

### Fixed

- Resolve the data directory independently of the install mode (`c48eb40`)

### Internal

- Stop tracking the data blobs in git (`1e0b98e`)

## 0.3.0 — 2026-09-20

### Added

- Warn when a frozen trunk has no embeddings store (`46cb6d3`)

### Fixed

- Make --limit mean N documents in every split (`0cc3361`)
- Drop the epoch framing from the frozen-trunk warning (`46b665f`)
- Pick the precompute forward's dtype the way training does (`7e34e2a`)
- Declare scikit-learn as a dependency (`1ddc896`)

### Performance

- Promote an embeddings-store hit into the CPU cache (`c819b8f`)

## 0.2.1 — 2026-09-20

### Fixed

- Take the padding fill from the logits' dtype, not a fixed -1e9 (`671a216`)

## 0.2.0 — 2026-09-20

### Added

- NERClassificationModel (`a6f2968`)
- Make document-logit pooling configurable (`9b680fd`)
- Make relation-loss class weighting configurable (`706efd3`)
- Promote entity_entropy_threshold and biaffine_hidden_size to ModelConfig (`c67e5a1`)
- Promote entity_entropy_threshold and biaffine_hidden_size to ModelConfig (`443f8e4`)
- Add Schema/EntityType/RelationType dataclasses (`f073a9b`)
- Distribute the BRENDA data blobs via the Hugging Face Hub (`a56cb7b`)
- Install through a pdm script that sync the active venv (`ceb8ed9`)
- Log per-epoch metrics to MLflow when a server is configured (`2da9138`)
- Stamp each MLflow run with its model and short commit (`2f18b49`)
- Log the run context, gradient norms and epoch timings (`f5bc3d5`)
- Log the test-set metrics that evaluate only printed (`8d8ede8`)
- Route the library's console output through logging (`07605fa`)
- Parse per-epoch wall times out of the training run's bars (`9c4a25d`)
- Read the precomputed embeddings store in get_token_embeddings (`f217d1f`)
- Report what the embeddings store actually served (`692845e`)
- Log how many documents an evaluation actually scored (`be7d0c1`)
- Let the VM experiment hold after a named stage (`e080bd1`)
- Check the encodings against the corpus before the VM run starts (`5bc4306`)
- Label tokens three ways from BRENDA's surface forms (`6f405f7`)
- Type the token targets, and ship a command that writes them (`c11403c`)
- Keep every mention's span beside the per-token codes (`75f0f93`)
- Train a token span tagger and link mentions to BRENDA IDs (`7b26c2f`)
- Run the token-supervision experiment on the VM in one command (`6180530`)
- Add a kill switch for compiling the model (`d1f9d8c`)
- Add a synchronous entry point for sync_doc_db (`46a2135`)
- Auto-namespace the MLflow experiment by commit (`a22d014`)
- Abstain a document-level class negative the text contradicts (`6f850c2`)
- Let a fuzzy near-hit abstain rather than assert (`a72f803`)
- Add a weighting choice to the span tagger's cross-entropy (`baa9a00`)
- Wire Schema into model construction (`898f5af`)
- Declare wordlist paths on the BRENDA schema's entity types (`cf84e9f`)
- Gate class_negative_abstention's cutoff per class (`7e135f1`)
- Gate bacteria's abstention cutoff separately in the ablation (`7d882ed`)
- Add class_negative_downweight as a graded alternative to abstention (`ac9178f`)
- Key every metric by its quantity, and ship a unit glossary (`c7aef25`)
- Warn at startup when torch ships no kernels for the GPU (`56b82c0`)
- Score linking against S800's taxids, not our own dictionary (`a0f8068`)
- Judge species linking on other organisms, not bacteria alone (`6ed2b06`)
- Judge enzyme linking against an outside EC nomenclature (`cb4c2a6`)
- Score the dictionary linker in an evaluation run (`6947985`)
- Score strain linking against culture-collection accessions (`4833232`)
- Score strain linking inside the evaluation run (`5b37e04`)
- Measure the token-loss weighting arms on the VM in one command (`70b50ec`)
- Measure how many microbiology papers name no enzyme (`8867301`)
- Measure whether torch.compile pays for this model (`05d4a7f`)
- Split span-detection recall by whether training named the entity (`f368cd0`)
- State numeric ranges in signatures with beartype validators (`b4ad43c`)
- Store every exact mention's candidate IDs in the token labels (`c1cc9cc`)
- Let the base model's top N encoder layers train (`89a0d99`)
- Ground a tagged span in the store's overlapping mentions (`4ce8a34`)
- Ground relation candidates in tagged spans, not the entity head (`469cc08`)
- Regenerate bacteria.txt from ncbitax, curated for isolate noise (`2276d70`)
- Filter enzyme candidates to zero-match survivors (`de82eb8`)
- Wire the enzyme-negative pool into train/val/test (`111e26b`)
- Count CPU embeddings cache hits and misses per pass (`1fbc78b`)
- Distribute the BRENDA data blobs via the Hugging Face Hub (`2c083ed`)
- Add a synchronous entry point for sync_doc_db (`85fc831`)
- Wire the enzyme-negative pool into train/val/test (`46149d0`)
- Record the gold corpus's digest on a linking report (`dd02b7e`)
- Log per-type entity unseen rate beside dataset metrics (`7c43e49`)
- Add an ambiguous flag to Mention, additive only (`d7a94ad`)
- Log a build's abstained-token rate, record the genuine share (`25a45b4`)
- Encode S800 and enzymeNER into the precompute-encodings store (`3bbf713`)
- Score linking from a tagger's own spans, not gold (`965e6aa`)
- Down-weight ambiguous comma-joined mentions in the tagger loss (`d8f2560`)
- Wire predicted-span linking into organism/enzyme/strain scoring (`57530c5`)
- Adopt trunk unfreezing in the best config and tuning sweep (`3989333`)
- Add a midpoint to the unfrozen-layers tuning sweep (`66f70a1`)
- Add a reversible key for S800/enzymeNER encoding-store groups (`fcc7b52`)
- Ground a tagger's aggregated-axis spans in char offsets (`5d4fcd9`)
- Wire a tagger's own spans into evaluate's linking report (`3a18140`)
- Record the seed, the release tag and the machine config on every run (`649f93a`)

### Fixed

- Accept but ignore entity-linking arguments in NERClassificationModel (`3a37cce`)
- Class_loss uses w_ent weight, not w_rel (`b992aa2`)
- Make Model.device configurable, default to cuda if available (`5941373`)
- Read NERCClassificationModel head dropout from config (`2401951`)
- Make entity_threshold a plain float and wire it into hard mask (`6686097`)
- Use relations_non_index in evaluate_model relation alignment (`e141618`)
- Pool entity token features with mean, not logsumexp (`3a62daa`)
- Dedupe merged relation pairs in ETEBrendaModel.forward (`18abe41`)
- Load legacy base-model configs lacking `model_type` (`ddf66a3`)
- Repair tuning entry point (`16b67d1`)
- Make batch_input_tensors return 2-D [sum(n_chunks), token] (`1ab62fc`)
- Skip pmids missing from HDF5 instead of aborting the batch (`33a2803`)
- Unify BrendaDataset int and list indexing schemas (`9a05a36`)
- Pad_offsets preserves integer dtype (`2774c24`)
- Repair and prune scripts entry points (`43cc9c6`)
- Package the pipeline CLI so its entry points actually runtime (`679c4a3`)
- Drain every in-flight embedding before pre-compute embeddings exits (`b2299ec`)
- Make precompute-embeddings honour --batch_size, --max_length and -f (`9aa82a7`)
- Make `pdm run tuning` runnable, and delete dead/debug code (`733e109`)
- Stop tokenizing "nan" into the encodings; extract & test the CLI helpers (`374aeca`)
- Stop every d3text command from writing lpsn.log into its cwd (`7b9c286`)
- Accept a Path, not only a str, as a DictTagger vocab source (`63bf4b0`)
- Commit tuning_config.toml, which .gitignore was swallowing (`0b8c5ff`)
- Assign class targets by position, not by index label (`982973d`)
- Order the entity index deterministically (`85b7836`)
- Flatten the DataLoader's collate dim in batch_input_tensors (`9118448`)
- Seed and partition the noise pool so splits stay disjoint (`958a6fb`)
- Restore the batch bar under the token-budget sampler (`c6a2d9f`)
- Record the epoch that produced the best validation loss (`e081679`)
- Record the head vocabulary in the checkpoint (`d08dd31`)
- Ship brenda_references' config.toml, take the DB host from env (`737686f`)
- Write regenerated splits where the package reads them (`5316973`)
- Take each progress bar's last frame, not its full one (`d5b1bc4`)
- Keep every wordlist entry in Vocab's length index (`5c3fd1f`)
- Mark a span two wordlists match equally as ambiguous (`4ba9582`)
- Drop a document whose encoding holds no token (`95cc217`)
- Drop a batch whose every document is missing from the encodings (`6373989`)
- Stop precompute-embeddings when the LMDB map runs out (`045cd2b`)
- Skip a document the chunk-budget sampler has no length for (`473af66`)
- Derive the wordlist length filter from the match cutoff (`c88c8ee`)
- Stop tracking the private database host at the repo root (`c9ac8a1`)
- Bench the forward the embeddings store is compared against (`ebc2fb2`)
- Make beartype optional in brenda_references' scripts (`499021d`)
- Skip encoding a document that has no text (`b24d19b`)
- Stop precompute-embeddings hanging when the writer dies (`171e4ee`)
- Initialise repr_sequence's accumulator rather than catch NameError (`7ec4796`)
- Skip a pmid the length-limited sampler has no length for (`7a799dd`)
- Encode a split filtered down to no row (`e3194dd`)
- Filter each relations dict rather than vetoing the row (`c957999`)
- Pool the class head per class, not with the entity setting (`375f31a`)
- Keep one unstrippable document from ending a corpus pass (`9d1af4c`)
- Pick bf16 only where the card has bf16 units (`6ff298b`)
- Pool both heads with logmeanexp (`3f6fef7`)
- Build relation targets from every relations dict (`8a83028`)
- Warn on a document shortfall with no batch lost entirely (`1d39b0d`)
- Stop the store's mismatch warning from naming a culprit (`f789925`)
- Run the VM experiment's preflight on every run (`09aae4a`)
- Let brenda_dataset take the unset --limit its callers hold (`5b3e272`)
- Drop the dead console-script entry points from brenda_references (`9b537e4`)
- Reject a --map_size that reserves no address space (`9620386`)
- Let Trainer take the model torch.compile hands back (`be1e69d`)
- Stop weakening nltk's ReDoS guard process-wide at import (`942bf53`)
- Match wordlist entries across case and punctuation (`ccf4cfc`)
- Score validation under the ramp's final loss weights (`c918dc1`)
- Exempt tag stripping from nltk's ReDoS guard, one call at a time (`18198c7`)
- Stop ordinary English words naming BRENDA entities (`af25a41`)
- Keep dynamo out of the runtime type checker when compiling (`c03f71f`)
- Restore the best epoch when a run exhausts num_epochs (`77c4c47`)
- Check a label store's recorded space when reading it (`dbae231`)
- Record which model wrote the embeddings store (`7665321`)
- Stop resetting the patience counter through the loss ramp (`dbed535`)
- Name the only cause a store/encodings row mismatch can have (`170c62e`)
- Correct what the --map_size refusal says LMDB does (`1987fc7`)
- Pass the model's device into get_batch_entities (`457a5d3`)
- Refuse a bad map_size or store before the model loads (`959c8d1`)
- Move the localization probe into the tracked tree (`7996547`)
- Remove base_layers_to_unfreeze, a knob that never unfroze anything (`9d434b4`)
- Count gold relations the entity head never proposed as misses (`9f5970c`)
- Don't ramp the entity-linking class loss on the relation schedule (`e0c21e2`)
- Make the batch a list of documents, not a default_collate accident (`a631a78`)
- Stamp the encodings HDF5 with its own geometry (`dd93474`)
- Wire separate_predicate_layer, drop the dead loss knob (`6ed2b18`)
- Drop the unread entities_in_batch parameter from ETE forward (`4e88ef1`)
- Repoint brenda_references/scripts/ imports at their new homes (`1f56e50`)
- Let ROCm's bf16 allowlist actually decide bf16 use (`d923f26`)
- Keep tune's per-trial config dump in field order (`e43bf4f`)
- Log the detection report an evaluation already returns (`770c864`)
- Dedup gold relation pairs before pooling their logits (`f8482c9`)
- Reject non-positive batch/commit/stream flags up front (`7cc4a1e`)
- Use torch.stack to give ground_truth its batch dimension (`d53795c`)
- Restore the ramp and missed-relation fixes the split dropped (`8d83ba1`)
- Remove entity_loss_scaling_factor from ModelConfig (`ce0dcf7`)
- Drop a removed field from three tracked experiment configs (`c357613`)
- Fail the dec04 detection stage instead of stamping it done (`4ee4acc`)
- Stack per-document class targets in NER ground truth (`d63a4d7`)
- Train a fresh baseline arm and stamp the commit (`f6f58c6`)
- Train and evaluate the ablation baseline arm fresh (`4b66f65`)
- Gate the class-negative abstention mask on span length (`dd2603f`)
- Report document count in a base-model provenance refusal (`cb0d193`)
- Drop BrendaDataset's dead embeddings override parameter (`89b0255`)
- Refuse a map_size too small for one document (`5605d2f`)
- Exclude the provenance key from store_coverage's key count (`9fb9b6c`)
- Reset the process-wide CPU embeddings cache between tests (`7155915`)
- Decode HDF5 pubmed ids before the resume membership check (`83bf774`)
- Normalise pooled logits by real token count, not padded length (`1ffa35c`)
- Log epoch count and stop flag without a validation split (`2d5ae20`)
- Join gold relations to candidate pairs in column order (`3bcf622`)
- Make the tree clean under the repo-wide ruff gate (`1d5fac2`)
- Reject misspelled normalization and lr_scheduler at load time (`1a98d36`)
- Refuse an encodings store built at another window stride (`375bba7`)
- Drop blank lines when reading a tagger wordlist file (`320e99e`)
- Reject unknown keys in the per-machine config.toml (`e4beeeb`)
- Count a repeated gold relation as one unproposed miss (`c18634e`)
- Key the CPU embeddings cache by base model (`77333f4`)
- Skip embedding a document that has no text (`7c7da3f`)
- Count and report the embeddings writer's deletes as deletes (`a48273c`)
- Print exception notes the stackprinter hook drops (`90a531f`)
- Count a repeated out-of-vocabulary gold relation once (`803286f`)
- Index other organisms under their abbreviated genus too (`cfeb8d9`)
- Stamp the surface-form index a token-label store was built from (`d18b90f`)
- Resolve organism taxids from the verbatim corpus names (`36eab42`)
- Pair a bacterium with its taxid by identifier before by name (`7e39689`)
- Read a thousands separator as part of the deposit number (`4da7b71`)
- Index a deposit number under both of its spellings (`af60d8b`)
- Keep a failing compiler backend from killing the run (`d2a2f08`)
- Record the label store a checkpoint's targets came from (`853b102`)
- Surface a backward-graph compile failure at the forward (`6a184b2`)
- Fingerprint the token ids an encodings store holds (`282fec1`)
- Stop ordinary words and acronyms from carrying entity IDs (`c4c0e8f`)
- Retag what the epochs ran when training dies (`a7bfd96`)
- Refuse to nest a writing pass on the encodings store (`03464e0`)
- Judge a benchmark arm by what its epochs ran, not by the install (`6c6f03c`)
- Digest an encodings store past a group holding no token ids (`9564595`)
- Document the per-type detection F1 metrics (`91c411c`)
- Skip an external linking corpus that holds no gold (`1b3de88`)
- Refuse a fuzzy near-miss for a word carrying no letter (`fd55b3a`)
- Stamp the rules that placed a store's token labels (`54152b3`)
- Register an EC number only where the text writes it as one (`06dcc61`)
- Bound ramp_epochs and three neighbouring numeric fields (`638e942`)
- Refuse a negative --limit instead of emptying the split (`1dd0592`)
- Reject dataset_splits shares that would go negative (`1b3434c`)
- Return relation coverage counts evaluate_model prints (`ac10d01`)
- Document the support-10 entity macro-F1 in the metric glossary (`f7f0a0a`)
- Stop ETEBrendaModel.__getattr__ from silencing mypy (`57ba4cc`)
- Reject an offset mapping that outruns the labelled text (`d571e05`)
- Drop letterless designations from the strain form index (`7e39037`)
- Budget the CPU embeddings cache in bytes, not documents (`892e25a`)
- Close three holes in the labelling-rules fingerprint (`aa645f3`)
- Pin the frozen base model to eval mode (`229bcfd`)
- Reject a negative --limit in evaluate's argparse (`3e659ff`)
- Let the residency benchmark pick and record its source regime (`b71e445`)
- Compare other_bacids against str ids by coercion (`de8a3c8`)
- Skip malformed records instead of crashing docdb lookups (`21fbe2a`)
- Skip a corrupt linking corpus instead of failing evaluate (`65329cb`)
- Check the labelling-rules digest at eval time too (`f23404f`)
- Pool gold relation reps from entity's own mentions (`c4a4858`)
- Build class-negative abstain mask on CPU before transfer (`cbbe494`)
- Import label_audit.py's build_index instead of copying it (`1d6cd80`)
- Wire novelty scoring to entity IDs, not type-only codes (`c0cabcd`)
- Remove unreachable Git LFS filter declarations (`9794574`)
- Restore rapidfuzz's version floor (`d1aae4d`)
- Prune relation candidates no relation type admits (`ea25dac`)
- Sort fuzzy buckets so their order does not track insertion (`7924d9b`)
- Reject entity-type prefixes that overlap (`e3feabf`)
- Stop indexing strain descriptors shared by anonymous records (`6f17515`)
- Drop genus synonyms from bacteria named below the genus (`5787923`)
- Index an EC number's older E.C. spelling beside the EC one (`179d4f0`)
- Refuse a match that ends on an abbreviated genus's initial (`ac200da`)
- Leave bare `Genus sp.` placeholders unabbreviated (`44cb215`)
- Fingerprint a pattern constant by its whole text and flags (`0a67f73`)
- Compare the prefix-to-code pairing a label store was written by (`2563248`)
- Stop indexing species epithets filed as strain designations (`d0e482e`)
- Fill doc_id as int64 so a batch can pass 256 documents (`8ceb476`)
- Return an empty split when dataset_splits drains its pool (`b553026`)
- Skip the linking block when its BRENDA inputs are missing (`c51b4e4`)
- Log an uncomputable entity metric rather than dropping its key (`edf8793`)
- Make bare placeholders name no entity, exactly or by near-miss (`854ea13`)
- Cache validation embeddings as tensors a training step can use (`0a4f94c`)
- Keep function annotations out of the labelling fingerprint (`59c3977`)
- Stop abstaining on a measurement such as 3,000g or 128bp (`676ecd2`)
- Score entity LRAP only over documents with a gold entity (`e078c13`)
- Read a spaced-out symbol as a symbol in the descriptive screen (`d661a3b`)
- Skip the linking block when a BRENDA input cannot be read (`78db4b2`)
- Rewrite a token-label group an interrupted run left unfinished (`93aa34a`)
- Stop the residency benchmark reading NaN or -0.0 as agreement (`6f18596`)
- Drop a bare culture-collection acronym from the strain keys (`0dfc1ca`)
- Refuse a flat offset mapping when labelling a document (`5924c15`)
- Order the relation none-fill's pairs like the typed keys (`c953166`)
- Log a NaN class micro-AP instead of raising out of evaluation (`ea0fc5f`)
- Await store_strains so sync_doc_db actually stores strains (`ff5185a`)
- Log organism names fix_taxonomy cannot decompose (`fdb0705`)
- Read designations too when building the strain bridge (`281a7ac`)
- Register a deposit's type-strain-marked spelling (`992704c`)
- Refuse to open a missing BRENDA doc database (`d3ac710`)
- Reject unrecognized BrendaDocDB storage values (`015cd21`)
- Exclude the psycholinguistics pool's enzyme-naming rows (`9028907`)
- Point build_organism_taxid_bridge at ncbitax's public DATA_DIR (`fb36682`)
- Parse each JATS article where it sits, no re-rooting (`be53e69`)
- Refuse a StrainInfo join whose designation isn't a registry id (`3c76146`)
- Match PMC efetch's own pub-id-type spelling for pmc_id (`3500c06`)
- Pass score_cutoff to rapidfuzz in Vocab.match (`e1c0c0a`)
- Break intra-Vocab score ties by scored text, not file order (`0d55cb6`)
- Charge TokenLabelReader's cache by real bytes, not entry count (`bdf2f4b`)
- Seed and partition the noise pool so splits stay disjoint (`25ad71a`)
- Ship brenda_references' config.toml, take the DB host from env (`34a9adb`)
- Write regenerated splits where the package reads them (`7285d5d`)
- Make beartype optional in brenda_references' scripts (`6ad2842`)
- Drop the dead console-script entry points from brenda_references (`6cf741c`)
- Repoint brenda_references/scripts/ imports at their new homes (`6ef9ae7`)
- Refuse a negative --limit instead of emptying the split (`83a634f`)
- Reject dataset_splits shares that would go negative (`258c739`)
- Compare other_bacids against str ids by coercion (`3f784c6`)
- Skip malformed records instead of crashing docdb lookups (`c645fdd`)
- Remove unreachable Git LFS filter declarations (`aa52bc4`)
- Return an empty split when dataset_splits drains its pool (`b998c27`)
- Order the relation none-fill's pairs like the typed keys (`dff6666`)
- Await store_strains so sync_doc_db actually stores strains (`36a4f5a`)
- Log organism names fix_taxonomy cannot decompose (`e87c783`)
- Refuse to open a missing BRENDA doc database (`b5671a4`)
- Reject unrecognized BrendaDocDB storage values (`0aa28ec`)
- Exclude the psycholinguistics pool's enzyme-naming rows (`1fd3f45`)
- Refuse a StrainInfo join whose designation isn't a registry id (`dbc8474`)
- Route __getitems__ through stored_ids for partial groups (`b6914fb`)
- Key writing_pass re-entry on inode, not resolved path (`6d3d486`)
- Drop dead entity-linking keys from compare_arms headline (`652f478`)
- Rewrite a torn encodings group instead of skipping it forever (`c5f87f5`)
- Merge split rows sharing one pubmed_id (`2661d5c`)
- Stop accession_spellings doubling a deposit's trailing letter (`b8a7dd6`)
- Merge rows sharing a pubmed_id before writing token labels (`5da2a76`)
- Catch OOM during the residency benchmark's equivalence check (`2321e1e`)
- Drop "constitutive" as a live BRENDA surface-form key (`abc3476`)
- Stop SurfaceFormIndex claiming a hash it cannot deliver (`0da38d8`)
- Drop unread, doubled-path entities key from brenda config (`b36572d`)
- Refuse DocumentLabels codes that contradict a gold anchor (`8e2b282`)
- Skip noise-pool load when noise count is zero (`0c0099f`)
- Reject overlapping ID prefixes in LabelSpace (`b40b536`)
- Drop archaea and protozoa as BRENDA placeholder forms (`cd94754`)
- Number doc_id after the survival filter in __getitems__ (`9d7357a`)
- Dedupe strain-bridge rows by entity and accession, not source (`7e81152`)
- Check relation-ID prefixes against the schema per entity type (`3711d2c`)
- Check the label space before extending a token-label store (`257be7b`)
- Drop brenda_references entry points that can't ship (`c524ab4`)
- Warn when train/evaluate read a token-label store built under moved rules (`567897f`)
- Exclude padding and special tokens from the abstained-token log (`36704c1`)
- Pin ncbitax to v0.1.4, resolve row-order taxid collision (`dbcbf72`)
- Fingerprint tuple, list and dict labelling constants (`4afe0d8`)
- Verify BRENDA inputs against SHA256SUMS before building the linking index (`3226e9a`)
- Don't let a collection acronym's truncated match hide a quantity (`fbc156f`)
- Refuse a fuzzy near-miss on a word the index deliberately dropped (`827d687`)
- Keep a genus-initial `sp.`/`spp.` abbreviation case-sensitive (`07a6659`)
- Give a genus known only through a synonym a pseudo-entity key too (`9ba9149`)
- Skip the linking block when a manifest omits a required input (`fdad3d9`)
- Size dataset splits from the sampling pool, not every document (`d396af1`)
- Raise on a manifest line missing the two-space separator (`f9bd953`)
- Fall back to exact gme sampling on pools no bigger than 20 (`f8b1d70`)
- Widen the organism exemption past a single abbreviated genus (`bbad585`)
- Drop the class-head OOS column by name in NERClassificationModel.evaluate_model (`7b3804c`)
- Point the tuning sweep at ETEBrendaModel, not NER (`5f60f9a`)
- Count LPSN ids dropped as contested in the taxid bridge report (`6f4a65d`)
- Use would_admit in the residency benchmark's cache write (`fbc233d`)
- Fingerprint labelling constants the walk used to skip silently (`85a5902`)
- Isolate the embedding cache write from dynamo tracing (`061bac5`)

### Performance

- Remove torch.cuda.empty_cache() from ETEBrendaModel training loop (`e3ccc2d`)
- Reuse _neg_inf buffer in ETEBrendaModel.forward masking (`c9b9190`)
- Cache one HDF5 handle per process in BrendaDataset (`a8d638c`)
- Vectorise relation alignment onto the device (`a3f33b1`)
- Stop recording the ETE hard-entity mask in the autograd graph (`ace5c33`)
- Keep the best-epoch state dict on the CPU (`a555caf`)
- Pool the token dimension without a float32 copy (`81d3dc3`)
- Slice the ETE hard-entity mask along the token dimension (`dc0a5d5`)
- Budget pooling slices by element count, not token count (`6ed556e`)
- Bench_store and perf_baseline measurements (`06c0530`)
- Store embeddings as bf16 through compress2, not fp16 pack_array (`a017ebc`)
- Cache validation embeddings, not only training ones (`f11c11b`)
- Set libver="latest" on h5py (`deed065`)
- Add a benchmark comparing embeddings-store backends (`41fde56`)
- Decode a stored embedding without copying it first (`dc5f410`)
- Compile the model in place so the graph is entered (`ae2d1c2`)
- Stream corpus batches from a single scan pass (`9e51956`)
- Memoize SurfaceFormIndex.fuzzy_ids and is_common_word (`c8622cd`)
- Stop autograd from tracking validation batches (`15d8385`)
- Enable loss scaling only on the float16 autocast path (`7ba9e18`)
- Stop building the split train and tune never read (`d8d2b79`)
- Encode label columns per column, not per document (`f6c55f4`)
- Stop copying the mapped page in the LMDB benchmark (`a2a0e55`)
- Aggregate token embeddings where the forward left them (`6b6b2cf`)
- Score only fuzzy candidates whose length can reach the cutoff (`b45bfc1`)
- Label token-supervision documents across a worker pool (`7c69d19`)
- Stop stripping JATS from a split column nothing reads (`ce852fa`)
- Block Vocab.match candidates with a trigram index (`cf96393`)
- Rule out a Vocab population by case before scoring it (`b6c63a7`)
- Rule out a Vocab population by case before scoring it (`a210016`)
- Stop checkpointing the whole trunk for a partial unfreeze (`30862e2`)
- Build Vocab's per-bucket trigram index lazily (`db91b60`)
- Memoize Vocab's per-length search-space bounds (`9010e70`)
- Make gradient checkpointing on the hidden block opt-in (`5784fcd`)
- Memoize tune's dataset build on (base_model, limit) (`1ffe6fb`)
- Share one hidden-block projection per training batch (`e8c832a`)
- Skip the host copy for a cache write that will be declined (`da7d788`)
- Decode label-store candidate ID sets lazily (`d7f045c`)
- Accumulate epoch losses on-device, sync once at epoch end (`1f8b5f6`)
- Merge empty-document drop and length walk into one HDF5 pass (`586f120`)
- Fold per-type detection rescans into one pass (`8e31c7a`)
- Shrink generate_embeddings.py's HDF5 chunk under the cache (`d1a5cbf`)
- Make d3text.datasets.__init__ lazy for BRENDA_SCHEMA/brenda_dataset (`fb83aac`)
- Prefilter windows in _mention_anchors by character reach (`7fa6847`)
- Share the span tagger's logits within a batch (`764de9c`)
- Memoise TokenLabelReader's per-document aggregations (`2456215`)
- Compute document_lengths once per batch, thread it (`e365855`)
- Read scored relation meta to host once per batch (`14ec86c`)
- Skip offset-mapping computation where it goes unread (`288893e`)
- Mask class logits in place instead of copying (`5a59084`)
- Read documents by position, drop useless pin_memory (`1ffce66`)
- Batch precompute-encodings' tokenizer calls (`2d7cfeb`)

### Changed

- Unify token pooling via logsumexp across all forward passes (`a79302c`)
- Unify eval relation alignment with the training aligner (`e0cc957`)
- Remove dead NERCTagger and annotation cluster (`b0a635f`)
- Derive model entity list from entity_index (`dd95e86`)
- Move import-time global state into runtime.configuration() (`4944b0b`)
- Derive the BRENDA dataset adapter from a Schema (`9ccd55a`)
- Read the sampler's lengths once, from HDF5 metadata (`8d4b895`)
- Stream compute_frequencies instead of stacking (`0e6f864`)
- Set batch_max_chunks (`243940a`)
- Vm config (`6eba9e2`)
- Return the matched term from Vocab.match (`49af054`)
- Let a VocabMatch carry several entity IDs (`abbe8c3`)
- Track the pooling experiment's driver under scripts/ (`c5f8d01`)
- Drop the dead memory-efficient-attention config write (`ae6d220`)
- Drop the unrunnable annotation-target script (`482aef8`)
- Delete the two scripts left over from the webapp layer (`810e366`)
- Hoist the training loop out of Model into a Trainer (`6f7ed26`)
- Return the checkpoint's parameters from Trainer.fit (`4e08cb0`)
- Split models.py into base/heads/ner/entity_linking/ete (`515fc8f`)
- Delete unused tokenize_cased helper (`7bfcf8f`)
- Set batch_max_chunks (`377d9e5`)
- Drop reintroduced unfreeze_encoder_layers and its dead branch (`4f033ae`)
- Drop dead branch and orphaned field from training-loop hoist (`9ce3e0d`)
- Drop train's unused torch._dynamo import (`f5027ca`)
- Narrow Trainer's model parameter back to Model (`c9e4018`)
- Replace three run_epoch copies with one shared loop (`610c8f9`)
- Compose ETEBrendaModel instead of subclassing it (`a82cb4c`)
- Delete VocabMatch.entity_ids, an unfillable slot (`5f29985`)
- Drop token_merge's unreachable None-offset branch (`233755a`)
- Drop the model config subclass no loader instantiates (`ddb399a`)
- Bring brenda_references back under ruff format (`840d9c6`)
- Share the embedding-path fixtures in the base-model tests (`996e9fd`)
- Name the relation label policy and support threshold once (`2a976e2`)
- Share the --limit argparse validator across CLI modules (`3e4a4ff`)
- Delete unused get_class_weights and its imports (`8d6addd`)
- Type token_labels' offset_mapping as ArrayLike, not Any (`bd707bd`)
- Share one letter test between the letterless-form guards (`e0c18ba`)
- Read token codes and entity presence through one projection (`331dc3c`)
- Drop the entity-linking head (`a6a410e`)
- Make docdb.py's Table.get typing and errors honest (`b4f6a1e`)
- Bring brenda_references back under ruff format (`c8fb996`)
- Make docdb.py's Table.get typing and errors honest (`41aa20f`)
- Extract merged-taxid forwarding into a shared helper (`a853338`)
- Route the species and enzyme scripts through linking_corpora (`0adf539`)
- Pin BatchItem's id field to an integer tensor (`aa53c85`)
- Name the frequency-clamp epsilon once (`af9c14b`)
- Drop dead defaults from BiaffineRelationClassifier (`c0be3d4`)
- Drop base.py's four uncalled members (`8260c7b`)
- Drop DictTagger, the tagger nothing in the pipeline builds (`5b7c646`)
- Drop dead upsample and its datasets import (`dc71cb5`)
- Size heads from the loaded base model, not a hardcoded table (`7720720`)
- Drop dead imports and locals from brenda_references statistics script (`989fb5d`)
- Remove dead evaluation flag on BrendaClassificationModel (`82e4f97`)
- Drop restating comments from NERClassificationModel.__init__ (`50d7e69`)
- Move code-to-prefix lookup onto LabelSpace (`79b0446`)
- Split Model.get_token_embeddings into three helpers (`77f103e`)

### Internal

- Point to the in_workspace branch of the submodules, define sources at the root (`b068d4c`)
- Migrate to pdm (`f931184`)
- Add brenda_references as subtree (`66e7a27`)
- Source local sub-packages from git; fix brenda_references deps (`312799e`)
- Unblock the suite and add test scaffolding (`3d5f670`)
- Add pure unit tests for models/utils/data/config/dict_tagger (`2ab22db`)
- Add fixture-backed BrendaDataset + forward smoke tests (`38b08d8`)
- Add opt-in GPU device-placement smoke tests (`2a5c204`)
- Fix ruff findings, unblock mypy, clear data.py/config.py (`68e7039`)
- Add CPU unit + nightly integratio workflows (`8c55ae0`)
- Add huggingface_hub dep to brenda_references (`7bdbc9b`)
- Drop uv and install through pdm alone (`054a2b6`)
- Add the regression tests for the pooling and batching fixes (`66496b8`)
- Bind compute_frequencies' aliasing and ragged guards (`237a3ed`)
- Correct what the pooling default was measured against (`74808b1`)
- Name the mechanisms the store's checks actually rest on (`92db6c6`)
- Pin the legacy evaluation branch's whole call again (`6d6ad67`)
- Stop the VM preflight reporting a dtype it no longer picks (`d252a16`)
- Pin a floor under every class channel's document recall (`53ec6ae`)
- Pin the span painting, space forwarding and stale deletion (`4377d49`)
- Read the relation report off the logger that now writes it (`91c82d2`)
- Bind Trainer.fit to a real model's run_epoch signature (`8200612`)
- Resolve config-write aliases in the base-model scan (`2c6cffe`)
- Give brenda_references its own test workflow (`f8ab6c7`)
- Mark test_expand_doc_gets_pmc_open as integration (`7715b74`)
- Catch .update() writes in base-model-config scan (`bdd27b1`)
- Resolve the imported name in from-import script checks (`d74f369`)
- Lift the datasets ceiling to <6 (`0b59bae`)
- Drop two dead-code ruff findings from prior review (`0ba3506`)
- Import the epoch-stats helpers from where the split put them (`6f01fa7`)
- Stop split_and_tokenize's window test hitting the network (`5262046`)
- Split test_models.py along the models/ package seams (`bdad355`)
- Add hypothesis as a dev dependency (`f01fde7`)
- Extend property-based tests past the embeddings codec (`7bb889e`)
- Pin ruff in the dev group instead of taking it from PATH (`4cb9923`)
- Pin the relation one-hot's columns to the schema order (`7d04e03`)
- Assert the pinned dev tooling is the tooling installed (`994b27f`)
- State the real reason DocumentLabels stores text_length (`4e24f51`)
- Pin the masked mean's gradient to the real token count (`5317f6e`)
- Move design rationale out of docstrings into the doc site (`caa5c3c`)
- Name the category nouns instead of reading the constant (`1b4107c`)
- Assert every config class is loader-reachable, not absent (`acb3508`)
- Grade the balanced-weight product under float32 tolerances (`815cfcd`)
- Pin that the screen's progress bar delivers every document (`d5b0f09`)
- Pin that a run's compile tag says what its epochs ran (`d948502`)
- Measure complexity with wily, outside the working tree (`e4ea3b8`)
- Pin the novelty split on span type, not span alone (`c112847`)
- Stage the collapsed batch instead of driving it through xmlparser (`fbbe6fa`)
- Add memray to the dev group (`4639a67`)
- Fit two token-label test docstrings to the prose rules (`322b80c`)
- Pin the constructor refusals of the token-label types (`1bf38f1`)
- Take the schema from d3text.schema in the surface-forms suite (`34b6e2c`)
- Drop weak align_relation_predictions tests, keep None case (`59f38d6`)
- Fix stated rationale for gold-relation-pair dedup test (`41e549e`)
- Check the embeddings store's stamp against what was embedded (`037876f`)
- Stop three embedding-path tests reaching a real store (`08c1979`)
- Parametrize the mask in the token-count aggregation test (`802126a`)
- Make the read-only writer case exercise the writer (`9de62e9`)
- Pin that focal loss mass-normalisation resists dilution (`03f2f3d`)
- Pin load_entity_tables' tail-seek branch against the whole-file route (`b0140eb`)
- Exercise the production pooling branch in _pool_logits (`94eb7a9`)
- Pin the lookup union and the form word ceiling (`2d0d265`)
- Split token-label tests along their banner comments (`c957730`)
- Pin the exponential scheduler's step to a falling rate (`b1bbcfc`)
- Explain how to read a near-zero relation score (`c654ecd`)
- Make the matmul-precision test backend-neutral (`b0a7955`)
- Correct git_commit's detached-HEAD claim (`87722c8`)
- Drop the timing deadline on a focal-loss equivalence test (`ac5462b`)
- Correct the linking index's cost from a 1.1 GB read to a tail read (`2333e69`)
- Name the export the strain-linking figures were measured on (`52915e6`)
- Drop the subprocess probe for entity-index sort order (`30090fe`)
- Key relation-alignment gold through the real column sort (`b487f16`)
- Pin ncbitax at v0.1.3 for the bacteria wordlist prototype (`0057f0c`)
- Correct entity-table read cost in precompute-token-labels (`b24b9ed`)
- Drop unused seqeval/ordered-set deps, relock (`f5a3ff3`)
- Pin enzyme_negative_pool.json in the data manifest (`e188025`)
- Re-pin enzyme_negative_pool.json after upstream fixes (`db92f66`)
- Pin DictTagger's shortest-span tie-break and str vocab path (`5456376`)
- Clean up test_dict_tagger's docstring and fixture helpers (`4079fa5`)
- Add brenda_references as subtree (`9bfa7a1`)
- Source local sub-packages from git; fix brenda_references deps (`94cb50a`)
- Add huggingface_hub dep to brenda_references (`79be45c`)
- Drop uv and install through pdm alone (`b2c55f4`)
- Give brenda_references its own test workflow (`9bbe866`)
- Mark test_expand_doc_gets_pmc_open as integration (`64071a7`)
- Drop two dead-code ruff findings from prior review (`ec58123`)
- Move design rationale out of docstrings into the doc site (`33585a2`)
- Pin enzyme_negative_pool.json in the data manifest (`15dac9a`)
- Re-pin enzyme_negative_pool.json after upstream fixes (`005b220`)
- Adopt brenda_references as subtree (`8680885`)
- Remove brenda_references to convert into a subtree (`2e5d0c8`)
- Restore surface_forms contract tests lost in 7e39037 (`f355aa4`)
- Attribute the enzyme score's lenient half to shared nomenclature (`974d341`)
- Pin build_index as the sole guarded surface-form entrypoint (`5e0e234`)
- Re-fetch after fix_taxonomy in test_29345379 (`2f9e035`)
- Stop taxonomy tests from writing into a tracked fixture (`f5e1424`)
- Stop overpromising the configured-source hit path (`e4dff96`)
- Pin runtime eager fallback's except clause and warning (`2c6c35f`)
- Pin configure()'s default seed to 42 (`01050bb`)
- Drop stale torch.compile workaround from VM runbook (`480759a`)
- Enable class negative abstention in best-known config (`2897a1b`)
- Warn --force-regenerate is required to repair pre-merge stores (`c11abd4`)
- Mark strain-ID retrieval test as integration (`39e3913`)
- Restore the backward-lowering flag between tests (`5a35e16`)
- Drop ticket citations from config.py comments (`cc0b68f`)
- Fix wrong offset-sharing and SWMR claims in _h5 docstring (`4889206`)
- Disclose unseen_rate's relation-argument scoping (`d6bd122`)
- Document BiaffineRelationClassifier and type its forward (`a3eced6`)
- Add docstrings to ETEBrendaModel's undocumented methods (`e9811c7`)
- Cover evaluate's training_entity_ids wiring through main() (`5daeab4`)
- Describe the current sp./spp./bacterium abbreviation split (`3e7aa90`)
- Add config traps, metric table, column conventions, module map (`838dd0b`)
- Split the site into how-to, reference and explanation (`52ed65f`)
- Build the docs site strictly on every push (`fe68308`)
- Publish the docs site to GitHub Pages on push to main (`c53cbe6`)
- Drop the starter Pages workflow (`bd94168`)
- Declare GPL-3, add a citation file, derive the version from the tag (`cc0180e`)
- Ignore generated stores (`63c613a`)
- Cut releases with git-cliff and a release script (`b11ec1a`)
- Lock a cu130 flavour for Blackwell, retire the ROCm one (`9c1bdf9`)

### refacotr

- Locate UNK/OOS columns by name, not by tail slicing (`efb2de6`)

### style

- Reformat assert calls per ruff's current style (`eadce93`)
- Clear the remaining brenda_references ruff findings (`b28d5fb`)

### surface_forms

- Give a bare genus with no genus record a pseudo-entity key (`3698841`)

### types

- Make models.py mypy-clean except for ETE Liskov overrides (`f396c05`)

## 0.1.0 — 2025-10-06

### Added

- Inform how many IDs we are retrieving from Entrez (`e1d341f`)
- Move unavailable PubMed IDs to a separate file (`278c7ab`)
- Add fulltext_retriever.py (`27698fe`)
- Add nice_retriever (`e4cc760`)
- Randomize delay between each retrieval (`8d9553c`)
- Run as a script (`9a46ed0`)
- Biolinkbert_large (`3336d54`)
- Entities, entities.datasets (`539e885`)
- General Model class (`1afc57f`)
- New species800 interface, with splits (`10d88ac`)
- Add progress bar (`e014e44`)
- Set self.ignored_index (`1ea5fe6`)
- Upsample the Strain label (`e5ce2dc`)
- Add version of the dataset only with species and strains (`a1d135c`)
- Hyperparameterize the model (`da52248`)
- Get_class_weights (`280fa8f`)
- Build the DataLoader in the model setup so batch_size can be set (`9f574a8`)
- Add class weights to the model specification (`daad755`)
- Hyperparameter tuning (`73d6e32`)
- Generalize the early_stop functio nto allow for other metrics (`ff5453d`)
- Class weighting with scaling and weaking of minor classes (`3a1b9ce`)
- Print model config before evaluation (`9864b2d`)
- PermutationBatchNorm1d: temporarily reshaping for batch normalization (`7759e22`)
- Setup k-fold cross validation (`de538b3`)
- Setting up k-fold cross validation (`a5c79eb`)
- Report loss mean and std accross folds (`ca5abc9`)
- Add verbosity flat to the evaluate_model (`fea40d7`)
- Add output_sequence option to evaluate_model (`4486a7d`)
- Add output_dict flag to evaluate_model (`2fd32c1`)
- Report average F1 for strains across folds (`d171ad9`)
- Report more metrics on log_config (`1ff5fda`)
- Add predict function to work with a trained model (`2200ebd`)
- Add get_loader (`f3d28a1`)
- Add tokenizer as a parameter of Model (`1eaf4c2`)
- Fill out the predict method (`ee841a6`)
- Merge_tokens now works with or without gold labels (`05522c5`)
- Add serialize_triples to convert token annotations to RDFa (`7cdea1f`)
- Return token offsets with predict (`6e34ed5`)
- Add predict function to work with a trained model (`e43a41f`)
- Add get_loader (`2c37735`)
- Add tokenizer as a parameter of Model (`ec09442`)
- Fill out the predict method (`348c00b`)
- Merge_tokens now works with or without gold labels (`d07295d`)
- Add serialize_triples to convert token annotations to RDFa (`015df97`)
- Return token offsets with predict (`74227c3`)
- Add xmlparser.py (`b185fe0`)
- Create database and app base (`cc13c27`)
- Allow for articles that don't have a PMID (`45799b1`)
- Add script to add new articles to the database (`540f5cb`)
- Return chunk of the source text, according to the given offsets (`1db424a`)
- Main segment retrieval function (`e217190`)
- Transform XML into HTML using XSLT (`a44957f`)
- Add stylesheet for serving article chunks with their metadata (`7dc6fb9`)
- Add Model.save_model (and document early_stop) (`154bc5a`)
- Add script train.py (`b3fa725`)
- Current hyperparameter choice (`254c5e2`)
- Parameterize the inclusion of test_split for the final training (`d7ba888`)
- Add backend_start script (`ea7df40`)
- Add functions for tag removal and reinsertion (`0821b39`)
- Add method to save model config (`d95c5f0`)
- Do inference on lowercased but return original capitalizatin (`ef37be7`)
- Basic annotator management functions (`0465a2b`)
- Return closing tags with the preceding char (`5ac6537`)
- Prevent multiple annotations for a chunk by the same annotator (`e2333a7`)
- Merge adjacent span nodes that refer to the same entity (`d0290e8`)
- Add function to retrieve all unannotated chunks for an annotator (`38ff63e`)
- Allow for the bulk addition of annotations to the database (`cf3b5c2`)
- Add script to annotate db articles (`78d8cf6`)
- Get_batch and response_to_article (`e7440e8`)
- Merge last chunk with the previous one if small (`4e98679`)
- Apply transform_tree prior to segmentation to keep title hierarchy (`1abede2`)
- Add functions to extract and copy vocab CURIEs (`8a52366`)
- Output a resource identifier to annotations (`be3cd8c`)
- Add annotation query (`97b0242`)
- Script to reset annotations (`43aaf01`)
- Combine resource names when tokens have the same string (`f435b37`)
- Combine also full and reduced bacterial names (`5f0ab9b`)
- Return the prediction probability for each label (`82f80f2`)
- Add a DictTagger to deal with undetected (but known) entities (`eb511b8`)
- Add dictionary tagging of bacteria and enzymes (`842bbbf`)
- Add script get_annotation_targets (`c3fe686`)
- Store_annotation API function (`44d032d`)
- Update_annotation (`9dcb1bc`)
- Add index_tensor function (`24cb8a4`)
- Multi_hot_encode_series_function (`b07a7e0`)
- ETEBrendaModel class (`bd007d3`)
- Add tokenization to BrendaDataset (`a43323a`)
- Export DatasetConfig from d3text.data (`12ab002`)
- Add ETEBrendaModel's compute_batch and forward methods (`938ff5b`)
- Data.get_batch_loader (`f11b7c3`)
- Use mean-reduced BCEWithLogitLoss (`df5b1a4`)
- LengthLimitedRandomSampler (`17628c1`)
- Add linguistics articles to the data splits (`ffd3b7f`)
- Save trained model at the end of train.py (`f77444c`)
- Add evaluate.py script (`191f1fa`)
- Add Model.unfreeze_encoder_layers (`c8f7e41`)
- Add compute_entity_frequencies (`f601aa8`)
- Initialize_classifier_bias method (`b7fc129`)
- Use logsumexp pooling on entity_logits and scale them (`b02c8ab`)
- ETEModelConfig (`271cdd6`)
- Add compute_entity_frequencies (`5e0356b`)
- Initialize_classifier_bias method (`8d5f192`)
- Use logsumexp pooling on entity_logits and scale them (`9e5e8b2`)
- ETEModelConfig (`9ca7445`)
- ETEBrendaClassifier (`aaf3eec`)
- Optionally return embeddings instead of BatchEncodings (`7dc4166`)
- Generate_embeddings.py (`48d480b`)
- Compute_batch (`54e2eb1`)
- Precompute_encodings (`2d3f0c8`)
- Load_tuning_config (`10176e7`)
- Scripts/tune.py (`a772369`)
- Add mapping from model to precomputed encodings (`01b3818`)
- Add map from model to embedding_size (`bc821cb`)
- Add --force-regenerate flag (`2024747`)
- NERCHead class (`01ba161`)
- ETEClassModel (`e555f1b`)
- Add relation classification to ETEBrendaModel (`e8a45ad`)
- Add layers to the entity classifier (`c69703a`)
- Add BiaffineRelationClassifier (`8738436`)
- Scaling ent_loss (`8c5022c`)
- Self.relation_label_smoothing (`d604f3d`)
- Compute_batch_true_x_pred (`8fce35d`)
- Diagnose entity and relation validation losses separately (`b1a4abd`)
- Check if stackprinter and beartype are installed before loading (`165ecfb`)
- Add aggregate_embeddings (`3373bb8`)
- Aggregate document token embeddings prior to caching (`98d5a88`)
- Gold assisted pairs for relation classification (`8254306`)
- Get_loss_weights to schedule the entity loss priority in early training (`e539407`)
- Scripts/precompute_embeddings.py (`b5c20f1`)
- Add _consistency_loss to penalize mismatches between ent and cls predictions (`f995949`)
- Initialize_classifier_bias and pos_weights (`c503a98`)

### Fixed

- Do not try to duplicate ids or invalid '-' ids (`0d8b077`)
- Do not count duplicate articles (`dc534b2`)
- Strip 'PMC' from the PMC ID (`6050ae0`)
- Increase delay to avoid rejected connections (`9473e3c`)
- Derive max_length from the BPE tokenized sequences (`da7b15d`)
- Provide the right number of labels to the one-hot encoder (`4e24560`)
- Include "#" in the list of NERC tags for the padding (`a5f3302`)
- Train_size -> test_size, dev_size -> val_size (`5944388`)
- Adapt load_dataset to the use of training and validation splits (`2972a34`)
- Convert the nerc vectors to float for training (`5ae07ee`)
- Reflect the change from datasets to data on __init__.py (`e8fada3`)
- No improvement should also count for early stopping (`9f4eefa`)
- Apply dropout when self.config.dropout (`2d2493f`)
- Reflect load_dataset -> preprocess_dataset change (`3413993`)
- Adapt to DatasetConfig format (`0d1763c`)
- Val_losses -> epoch_val_losses (`e126a8b`)
- Update validate_model to conform with the new parameter spec (`02c05e8`)
- Add a test_data argument to model_evaluate (`fa2ad95`)
- Clean the dynamo cache before recompiling (`116405f`)
- Initialize best_score with the initial val_loss (`e4a4705`)
- Typos (`dcb3b03`)
- Only check if metric is the best if best_score exists (`39e2a4d`)
- Predict method (`e549c16`)
- Adjust the typing of merge_tokens (`6d4c5f6`)
- Training should return val_loss only when validating (`850f312`)
- Reset dynamo cache before loading another config (`d50b4a8`)
- Reset dynamo cache before loading another fold (`1392c36`)
- Predict method (`afbbcac`)
- Adjust the typing of merge_tokens (`6cfff14`)
- Training should return val_loss only when validating (`0ac3c21`)
- Use absolute imports for datamodel to fix InvalidRequestError (`d370644`)
- Do not strip text when converting ElementTree to str (`12f43d4`)
- Allow connections from the frontend (`dc03664`)
- Add a root node so the output of get_chunk is valid XML (`b99805c`)
- Allow for start=0 in show_segment (`ef8242d`)
- Resolve database path relative to the module path (`3d9b19a`)
- Select chunk using the pmid foreign key (`e011a8d`)
- Reinsert the original tags in the right scope (`3ef7cbf`)
- Close tags properly (`de20280`)
- Update tests to expect annotation at the leaf level (`b116d57`)
- Update insert_tags to add annotations to leaf nodes (`f18ebe0`)
- Get the correct file path for saving the model config (`6b0afde`)
- Set dropout, hidden_layers, hidden_size, patience to NonNegative (`ded0c40`)
- Self.config.classes as list, since ndarrays are not TOML serializable (`f9016da`)
- Return multiword entities with a single id (`8c3577c`)
- Close html attributes and use forward slashes to close tags! (`df7f18a`)
- Add split_metadata_body to xmlparser (`857fe40`)
- Avoid overlapping xml elements (`192a31d`)
- Use lxml's incremental parser to add annotations in the right places (`7f0be90`)
- </ should be matched before < in the if block (`899007e`)
- Return closing tags with the preceding character, when there is one (`0d62060`)
- Extract article-title, not the whole title-group (`5958b34`)
- Add div as a subelement of chunk-body (`123b171`)
- Change <title> tags to <h3> tags inside <chunk-body> (`b94726b`)
- Use lxml's incremental parser to add annotations in the right places (`3bf0b7b`)
- Split, process and merge articles longer than the context window (`f5ab502`)
- Make loading of config.ini path-aware (`cb1fa64`)
- Respect len_threshold and split abstract (`e28405b`)
- Allow start to be 0 in get_chunk (`9f2d236`)
- Preserve BERT's "##" markers in tokenized_cased (`b07b2e4`)
- Reinsert tags in the right order (`d1232ef`)
- Impose some limits on the allowance for discontinuities (to be revised) (`f808b0b`)
- Preserve spacing for I- tags (`98f4ea3`)
- Prevent duplication of content (`d30d61b`)
- Do not repeat the main title as a segment of the article (`3eb1f98`)
- Guard against string inputs to lxml's fromstring (`f55590e`)
- Ignore metadata when extracting segments (`5505e5e`)
- Keep track of title hierarchy (`bb0629e`)
- Get title formatting in the XSLT (`daa8dc8`)
- Add a wildcard to the match pattern to make mypy happy (`26a86c0`)
- Simplify get_chunks and support tables and figures (`b5034eb`)
- Update get_text to reflect the new data model (`551a16d`)
- Include apply templates to all descendants of <body> (`ee386c1`)
- Remove namespace prefixes from the extracted chunks (`b5b696e`)
- Insert the div containing namespace declarations at the top of the annotation (`8e4414b`)
- Make <chunk> and <annotation> equivalent for the XSLT (`b6dfeac`)
- Update db.apy to conform to the new data model (`5a7c8b1`)
- Add relevant annotation namespace information at the top of the chunk (`3a7f230`)
- Add relevant annotation namespace information at the top of the chunk (`dcea2c8`)
- Make sure the merging of annotated tokens does not create duplicates (`0d7b151`)
- Count number of added annotation accross batches (`9df8deb`)
- Correctly parse figs (`ab46047`)
- Ignore BERT's special characters when combining predictions (`9ef655c`)
- Ncbitaxon -> d3o, Species -> Bacteria (`16dd0f1`)
- Add ending slash to <hr> elements (as required by XHTML) (`88570b6`)
- Add strains.txt and remove duplicates from enzymes.txt (`353a5f1`)
- Include the entities files in pyproject.toml (`e2ea894`)
- Correct config and utils imports (`d675500`)
- Correct typing for ETEBrendaModel's classes argument (`447428b`)
- Several import fixes (`d0ff4a5`)
- Import utils from d3text - and several formatting fixes (`8b14b69`)
- Make BrendaDataset compatbile with BatchSampler (`82c4961`)
- Get integer from tensor for indexing docs in BrendaDataset (`5c61168`)
- Chain the BatchEncoding lists from each document in the batch (`0af11c8`)
- Remove the spurious indentation of the forward method (`9f60b29`)
- Adapt the training script (`91ac0dd`)
- Move entity identification mask to correct device (`e62153b`)
- Make sure tensors are on the same device and initialize entity outputs (`4654deb`)
- Remove the initial value from the accumulator of doc_indices (`e65251e`)
- Adjust all the shapes in the train_model method (`6cb34b4`)
- Make sure utils.split_and_tokenize returns torch tensors (`f485e6e`)
- Do not use full graph compilation, for now (`7d7694c`)
- Collect entity predictions across the whole test data (`ac52d17`)
- Improve training loss calculation and tensor handling in ETEBrendaModel (`aa4299d`)
- Include the "none" class in the class classifier (`ead541d`)
- Add labels for multilabel classification_report (`9a6c4e5`)
- Add more feedback and fix class predictions on evaluate_model (`f22c97b`)
- Correctly set the number of layers to unfreeze (`49785cc`)
- Treat entity ids as strings (`d7e8dbd`)
- Add a prefix to each entity, corresponding to its entity class (`5579bcd`)
- Correctly set the number of layers to unfreeze (`bab734d`)
- Treat entity ids as strings (`f4c2159`)
- Add a prefix to each entity, corresponding to its entity class (`0d403a9`)
- Make sure input is int (`3657dde`)
- Pad to max_length to make sure singleton batches are padded (`2767609`)
- Choose the right index for cu118 (`4b24228`)
- Add hook to handle unexpected keys when loading model state dict (`9a82eaf`)
- Correct bert-mini encodings file (`69ed524`)
- Export log_config (`e2a5552`)
- Export log_config (`909b650`)
- Correctly check if a group has keys in the hdf5 file (`bec8589`)
- Parameterize output file name (`a86970c`)
- No_grad when processing base_output (`9dfaba2`)
- Stack input tensors correctly (`904cca3`)
- Improve indexing of sequences in the batch (`0f27138`)
- Make sure all relation predictions have 1 dim before stacking (`a568877`)
- Receive relations as one-hot encoded from brenda_references (`0def0b2`)
- Adjust parameters to facilitate backpropagation of rel loss (`4cf3a40`)
- Match predictions to targets using the entity index when computing relation loss (`8ca82c9`)
- Let gradients flow properly from rel_loss (`3fec7e4`)
- Recover gracefully from OOM (`2beae2e`)
- Return entity_logits to the computation graph (`1efdfdc`)
- Only save validation run when it succeds (`5cca6ef`)
- Only classify relations involving distinct entities (`550ea3f`)
- Add _pack_key to make key packing consistent (`6bc07aa`)
- Penalize relations involving false entity predictions, connecting the computation graph (`8b8d916`)
- Only penalize extra entities during training (`83c9916`)
- Profile with SequentialSampler (`e84420a`)
- Fix rel_index/rel_logits typing issues (`de82581`)
- Do not scale ent_loss on validation (`ee183f6`)
- Set cache sizes to NonNegativeInt (`9718c52`)
- Uncomment model saving (`bc928de`)
- Remove limit on data.brenda_dataset call (`6f2e289`)
- Truncate dataset after extracting entities (`aeef10f`)
- Squeeze truth tensors in the return of compute_batch_true_x_pred (`ac1520d`)
- Compute relation classification scores when there are relations (`41ec343`)
- Ensure y_pred is not a scalar (`2c229a5`)
- Save aggregated doc embedding to cache (`559762f`)
- Count supports across batch using indicator matrix for multilabels (`214853f`)
- Make sure ETEBrendaModel.ground_truth should outputs 2D tensors (`eb30ff2`)
- Prevent empty relation arrays from compute_batch_true_x_pred (`1f810f8`)
- Step only when there is a scheduler to step (`faac1b9`)
- Access the classifier's last layer's bias in initialize_classifier_bias (`e9748b2`)
- Return None from forwards when _compute_relations_vectorized does so (`bd13642`)
- Replace deprecated autocast call (`61e1bd6`)
- Make sure the early_stop metric is properly initialized (`4c2bc98`)
- Log best_val_loss when tuning (`a83e38a`)
- Prevent unsupported ScalarType BFloat16 (`c0ff35b`)
- Make sure relations_true and relations_pred are aligned (`866b26a`)
- Make encodings source explicit (`0cc2bf3`)
- Select dtype for autocasting dynamically, depending on bf16 support (`750bdab`)
- Do not use cuda_embeddings_cache when size is set to zero (`3a35187`)
- Only limit the size of the training data (`60e6df9`)
- Cast logits and labels to float and int, respectively (`3aa6ed5`)
- Do not flatten predictions before computing loss across batch (`74da507`)
- Class_ent_loss -> epoch_class_loss (`7a24271`)
- Keep loss accumulation on the same device (`710aab9`)
- Print epoch stats once for each step (`499d3fb`)
- Remove last position from id_logits and cls_logits before comparing to gold truths (`e16db9f`)
- Add freq parameters to model call in tune.py and train.py (`e506184`)
- Compute class labels from entity_cols so as to keep labels for UNK entities (`a700529`)
- Compute epoch losses and updates from scaled losses (`b813963`)
- Manage memory better while precomputing embeddings (`53c65c2`)
- Adjust shapes in ETEBrendaModel.evaluate_model (`cdfe49f`)

### Performance

- Reduce-overhead mode is the most efficient one here (`a71cb84`)
- Optimize for asynchronous data loading (`be9d8d6`)
- Memory optimization (`af98466`)
- Memory optimization (`86aa7a6`)
- Faster implementation of dict_tagger using fuzzy sets (`26fad0e`)
- Set matmul_precision to medium (`8159e53`)
- Pool across tokens in the forward method (`2c6751c`)
- Tiny memory saving on compute_loss (`bca2495`)
- Cache base model results (`224a535`)
- Optimize cache management (`cf0d40e`)
- Optimize validate_model (`ae6ab81`)
- Optimize vector operations for relation classification (`de36268`)
- Further vectorize relation processing and cache dummy logits (`2e035d7`)
- Further vectorize relation processing and cache dummy logits (`8d42989`)
- Cache embeddings as fp16 (`8acf383`)
- Slight improvements by using bfloat16 in autocast (`927417f`)
- Remove half conversion on logits and set num_workers=2 on the data loader (`129142a`)

### Changed

- Attach dataset to the model object (`a708cd4`)
- Using BioLinkBERT-base for now (`386273d`)
- Freeze the base model layers (`7d5f8e1`)
- No need to return tensors here (`4f6dc6f`)
- Set criterion as object parameter (`69fe047`)
- "sentence" -> "sequence" (`aba2b3a`)
- Utils.py (`10a81ac`)
- Decouple dataset loading from the model specification (`304886f`)
- Move batches to GPU only when they are needed (`a15c36d`)
- Tokenize_and_align to utils.py (`3f0c00e`)
- Rename datasets to data so as not to conflict with HF (`d7ea420`)
- Removing unnecessary argument form load_dataset (`66cfa4c`)
- Improve the visibility of utils for the other modules (`ea2afce`)
- Scale the feature nerc feature weights (`88cffd5`)
- Move tuning.py to the root of the package (`ce5ab30`)
- Move ModelConfig to utils.py (`cf94dc5`)
- Updates to the hyperparameter space (`0c374c4`)
- Split the classifier from the head (`a697c34`)
- Do not upsample and test each config 4 times (`d0e8a42`)
- Keep OOS label (`3c1cd86`)
- Add 1 to softmax (`d9eb5c6`)
- Reducing parameter choices to the most promising (`d127c96`)
- Factor out dataset loading from data preprocessing (`6ebdc1c`)
- Provide num_labels as a parameter of NERCTagger (`0c0e696`)
- Remove train_losses from cross-validation reporting (`654f9cd`)
- Import utils (`83fc2bf`)
- Update validate_model to accept a validation dataset argument (`6d1f99d`)
- Factor config printing out of the model (`b9f5c8f`)
- Factor checkpointing out of early stopping (`b59dce0`)
- Exponential scheduler (`1590224`)
- Remove training loss reporting from cross-validation (`97fc9cd`)
- Tweaking hyperparameter space (`59e6bd7`)
- Logits_to_tags and ids_to_tokens (`ac3fb2f`)
- Encode max_length as a property of DatasetConfig for now (`5146486`)
- Tweaking hyperparameter space (`f521dc5`)
- Logits_to_tags and ids_to_tokens (`7c19e67`)
- Encode max_length as a property of DatasetConfig for now (`ecb048f`)
- Add a textchunk table (`92a252c`)
- Manage database creation outside of the app (`6bc9748`)
- Move parsing functions to xmlparser.py (`efcc0a1`)
- Parameterize database file on feed.py (`015991c`)
- Let get_chunk accept str arguments (`aeeed5d`)
- Use NCBITaxon for the taxonomic terms for the time being (`6ee949c`)
- Add base_model as an attribute of ModelConfig (`dd6c30e`)
- Rename pubmed_article.xsl -> pubmed.xsl (`bd09385`)
- Add overloaded query function, calling transform_article only once (`0804d1e`)
- Merge adjacent tags in get_tags (`56a1bc2`)
- Specify the number of labels via ModelConfig (`e991d0e`)
- Use a pydantic model instead of a dataclass (`9ab9a64`)
- Import ModelConfig and other utils directly (`92a9448`)
- Make ModelConfig initialization order-independent (`31edebd`)
- Replace num_labels with classes in ModelConfig (`fef2fd2`)
- Use inline RDFa instead of standoff annotations (`399692d`)
- Make primary keys non-nullable (`c91f881`)
- Make query uniformly return a Response object (`20f774b`)
- Move utils to the top level of the project (`e018b4a`)
- Clean up (`cd08dff`)
- Do not count section titles for the length of a chunk (`e7e4f11`)
- Factor tree transformation out of transform_article (`f5ce655`)
- Update the data model (`554d718`)
- Add build_chunk create TextChunk rows in one place (`5469e5b`)
- Label annotations as such (`0b66912`)
- Remove unused argument in serialize_triples (`0651cdc`)
- Avoid uninitialized xml variable (`1a37e0c`)
- Factor namespace cleaning out of segment_to_string (`7181e68`)
- Exclude tests from the files watched by uvicorn (`3e12878`)
- Prettify the content of chunks just to make debugging easier (`11980e4`)
- Return an iterator from get_batch (`7b1b784`)
- Allow Element and Element tree in clean_namespaces and transform_tree (`9b2c247`)
- Add force argument to allow annotation overwriting (`4716118`)
- Return an iterator from merge_predictions (`1c4503e`)
- Generalize Model.predict by factoring out get_predictions (`6545e5b`)
- Receive annotation updates as form data (`5057bd8`)
- Split add_annotation and update_annotation (`8fbe847`)
- Renaming entities -> src (`2644d8a`)
- Remove explicity build targets for now and restructure the folders (`673fdd7`)
- Remove xmlparser and backend.datamodel (`11babb9`)
- Build indices for each entity type (`677bfd0`)
- Models -> d3text/models (`f0ea571`)
- Rename entity_class_map as class_map (`7136de0`)
- Move default config initialization to the Model class (`d2168c6`)
- Move freezing of the base model to the Model class (`0a0ab5c`)
- Move ModelConfig to models.py (`10dbf97`)
- Replace hidden_layers and hidden_size with hidden_layers as list of sizes (`dbc36dd`)
- Move utils to the d3text namespace (`3547d14`)
- Add max_length argument to utils.split_and_tokenize (`e3edf35`)
- Declare d3text.utils exports (`6650ec9`)
- Move model config to models.config (`cc86093`)
- Add model_class field to Modelconfig and create test config (`745717a`)
- Move common model layers setup to base Model class (`17acb6e`)
- Refactor batch processing out of train_model and improve reuse (`c52b92c`)
- Take a DataLoader argument in train_model (`fe80d77`)
- Improve entity classification routing with mask-based tensor selection (`37a07b6`)
- Improve tqdm output (`5a95d37`)
- Use data.get_batch_loader instead of building a loader in train.py (`9248397`)
- Expect DataLoader in validate_model (`271510b`)
- Move optimization out of compute batch (`1256f47`)
- Add self.scaler so we don't need to keep passing a scaler argument (`3e99338`)
- Make loss_fn a property of the model (`02e0b2a`)
- Compute average validation loss more efficiently with torch.mean (`489d9ab`)
- Improve stdout feedback of model training (`3256329`)
- Factour loss computation out of compute_batch (`fcdb0e1`)
- Memory optimizations (`1af0e3f`)
- Slightly more memory conscious loss computation (`641f2f7`)
- Parameterize batch_size on train.py (`5e801bc`)
- Improve tqdm feedback (`7f2e576`)
- Simplify self.best_score check and update (`6aabee0`)
- Move autocast environment to the forward pass (`085292a`)
- Make evaluate model a required method for Model subclasses (`6adefa9`)
- Little model overhaul, using a single entity classifier (`114b9e0`)
- Extract training setup logic and improve model documentation (`99d643d`)
- Update training script with increased dataset limit and validation data handling (`2bdeb19`)
- Remove unknown entity for now. It is implicit in a zero-vector (`0ed0d54`)
- Unfreeze some layers of the base model (`9e210a0`)
- Use GELU activation (`edd1da5`)
- Remove compute_batch from NERCTagger (for now) (`c5aba2d`)
- Leave it for the calling function to set no_grad on the forward method (`6ec5cb9`)
- Minor changes to the test config (`84e9d5d`)
- Make entity_targets non blocking (`d1dbad5`)
- Move backend code to its own package (`210cd8e`)
- Max_length = 1000 (`edc7f14`)
- Pruning some hyperparameters (`dc208b7`)
- Leave it for the calling function to set no_grad on the forward method (`874a1c1`)
- Minor changes to the test config (`19d7a02`)
- Make entity_targets non blocking (`f6a0d9d`)
- Move backend code to its own package (`57176e1`)
- Max_length = 1000 (`c5abdb7`)
- Generalize ETEBrendaModel to BrendaClassificationModel (`3901965`)
- Factor ClassificationHead out of ETEBrendaModel (`9828592`)
- Remove unused max_length parameter (`4bcc6b7`)
- Adjust noise parameter (`95d858c`)
- Do not initialize the base model in the main class (`e87ea77`)
- Adjust test config (`3d27ff6`)
- Precomputing embeddings was a bad idea, do not (`2f32c76`)
- Adjust class_scale and num_epochs (`79041c5`)
- Move on gracefully on old GPUs that don't support Triton (`980dc50`)
- Optimize model training with half precision and pooling improvements (`c4f5869`)
- Replace sequential hidden layers with module list and sequential layers (`28b9ac0`)
- Enable gradient checkpointing (`27a6c31`)
- Add encodings as parameter (`f1dc15a`)
- Improve the logic of document indexing (`1827fe0`)
- Tune with limit=1000 (`513b06a`)
- Only add to cache the newly computed tensors (`6b84aaa`)
- Print cache stats at every epoch (`f6d6a3c`)
- Remove memory pinning for now, but profile that on the two archs (`44f6a0c`)
- Factor get_token_embeddings out of compute_batch (`92447c8`)
- Factor pool_logits out of compute_batch (`b2b368f`)
- Factor get_pool_fn out of pool_logits (`3ee14af`)
- Update noise parameter (`cb2b08d`)
- Limit dataset to the first 935 samples for tuning (`7f4f70b`)
- Add generic type and protocol to BrendaClassificationModel (`7ade5e0`)
- Update type hints and add loss computation protocol for Brenda models (`69c6a35`)
- Factor common methods out of ETEBrendaModel and into BrendaClassificationModel (`3e86423`)
- Compute loss inside compute_batch (`ba100ec`)
- Clean up and set cache size for the VM (`fbacde3`)
- Show loss in decimal repr (`b2f61e9`)
- Softmax entity selection for relation classification (`3144568`)
- Set a unique threshold parameter for now (`644c0b7`)
- Update model logging and add performance profiling decorators (`ae7c336`)
- Profiling prep (`97fccd7`)
- Remove ent_logit scaling, add ent_loss scaling (`963b175`)
- Make entity_loss_scaling_factor a hyperparameter (`7ef6c7e`)
- Factor cache config out of models.py, into config.toml (`97a0e03`)
- Eliminate unnecessary calls to _dummy_relation_logits (`955d0e5`)
- Try to compile with triton when not profiling (`512e0f1`)
- Aggregate entity tokens for relation classification (`d3253e1`)
- Compute_batch -> compute_batch_losses (`6567a69`)
- Remove position tracking from _compute_relations_vectorized and fix several typing issues (`744095e`)
- Factor align_relation_predictions out of compute_relation_loss and fix evaluate_model accordingly (`66f5df4`)
- Parameterize xfp accuracy and lower tokenization stride (`e16e5b3`)
- Remove preprocess_dataset (`c1d6bd7`)
- Remove get_loader (`fc76a78`)
- Remove flatten_entity_indices (`b6bc9da`)
- Remove multi_hot_encode_columns (`47500d4`)
- Remove keep_only (`c146adf`)
- Fix typing issues (`661d4af`)
- Move embed_document to utils (`2e6d2d2`)
- Do not detach embeddings in embed_document (`af9d164`)
- Classify using document-level embeddings (`847840b`)
- Loop over unique_doc_ids and only consider 50 entities (`ebfa2eb`)
- Set save checkpoint to True by default (`e17c5e4`)
- Save self.device (`7ff00fe`)
- Simplify token attention mask creation (`28555ce`)
- Loop over unique_doc_ids and only consider 50 entities (`0f42228`)
- Loop over unique_doc_ids and only consider 50 entities (`1e8cd64`)
- Set save checkpoint to True by default (`d3c283c`)
- Save self.device (`fe61de0`)
- Loop over unique_doc_ids and only consider 50 entities (`7e6df0c`)
- Stop computing dummy relation logits (`265cfeb`)
- Noise=100 on validation data (`1c43391`)
- Use combinations instead of permutations to respect layer size ordering (`b4312ce`)
- Allow 3-layered hidden network (`c7ccf42`)
- Remove dataset limit for tuning (`048d4df`)
- Reduce CUDA memory usage during validation (`82e570f`)
- Early stop only after the entity ramp up (`bb4a3f6`)
- Parameterize the n of features in the hidden layer of the classifier (`65f480b`)
- Reduce the size of out_features in the hidden layer of the rel classifier (`2a7fdd6`)
- Use only one hidden layer outside of the classifier heads (`9869638`)
- Increase the number of hard detected entities (`55f3b19`)
- Add more architectural and training parameters to ModelConfig (`5baa431`)
- Pass the limit parameter to the dataset loading functions (`cb437db`)
- Increase training size limit (`991c63a`)
- Increase training size limit (`79fa604`)
- Evaluate model from pooled logits (`35fa693`)
- Assign zero weight to the none entity and to the none class (`08b7db5`)
- Factor run_epoch out of train_model (`8c1667a`)
- Factor common classification code out and into BrendaClassificationModel (`8bfa7e1`)
- Factor common classification code out and into BrendaClassificationModel (`8719447`)
- Add UNK entity and OOS class (`5b89d98`)
- Use softmax to select entities in forward (`41dfbac`)
- Use entropy to select confident entity predictions (`06f5eba`)
- Clean up (`27bb9b4`)
- Factor out common run_epoch code (`713d3b4`)
- Add common forward logic (`af76dc4`)
- Add stdout feedback to tune.py (`97e10d4`)
- Del cuda variables (`2dbef46`)
- Sample only 250 configs for hyperparameter tuning (`36f23bc`)
- Remove unnecessary loss sum (`fcfacbd`)
- Make sure we return a tensor from run_epoch (`b3470a5`)
- Remove unnecessary autocast contexts (`8dd38dc`)
- Add default weights to validate_model (`982c56d`)
- Factor backward pass out into _update and apply it by batch (`f8fdff8`)
- Keep all caching on the CPU (`a5f4951`)
- Add some more iterations to profile better (`6cac1cf`)
- Register _neg_inf as a buffer (`53f2fb3`)
- Factor out common validation code and use run_epoch in validation (`e3c9b1f`)
- Batch segments for computing embeddings to avoid memory spikes (`7969d4c`)
- Set attention_mask as bool from the start, move tensors to cuda late (`4b2dac8`)
- Precompute_embeddings with lmdb and blosc2 (`ccc2962`)
- BrendaClassificationModel.evaluate_model (`f2e904f`)
- Add limit as training parameter to the scripts (`950ad34`)
- Add consistency_weight parameter to ModelConfig (`c0a5a89`)
- Move get_loss_weights to Model and reweight class labels (`b7c4cb9`)
- Filter relations so that only those containing trained entities are considered in validation and evaluation (`5a0bd6a`)
- Adjust ETEBrendaModel to the BrendaClassificationModel refactors (`0d350da`)

### Internal

- List of unavailable PubMed IDs (`3d8ed85`)
- Add requests (`00f31b3`)
- Remove print debugging (`a09925e`)
- PyTorch and HuggingFace datasets (`00a48d9`)
- Generalizing type annotation to Iterable (`ffc51f8`)
- Pyproject.toml (`4a3e816`)
- Bratparser and s800classed dependencies (`c7847e4`)
- Bump Python version for compatibility with bratparser (`f942b23`)
- Fix license name (`4fbd5fb`)
- Entities/__init__.py (`bf47882`)
- Dependencies (scikit-learn, numpy, s800classed) (`d33b905`)
- Use Python 3.11.x, for torch.compile (`a52d1ef`)
- Add stanza and seqeval dependencies (`ebc1520`)
- Datasets/__init__.py (`e364ca2`)
- Entities/py.typed (`fd0050e`)
- Add docstring to merge_tokens (`b79a830`)
- S800classed to develop mode (`b7a7603`)
- Remove unnecessary dependency (`3ae4904`)
- Utils (`deaf7d6`)
- Utils (`75aafe5`)
- Make utils visible at the root of entities (`7fd7092`)
- Cleanup (`6852a29`)
- Annotate log_model (`7010089`)
- Style (`9b52d55`)
- Formatting (`5f5c62f`)
- Formatting (`ec2d814`)
- Formatting (`1dec296`)
- Better formating for console output (`ab3280d`)
- Type annotations (`d58e5eb`)
- Fix type annotation on train_model (`0ec3571`)
- Reducing minimum python requirement (`7112613`)
- Provide torch cpu and gpu dependency groups (`50f51bf`)
- Downgrade back to torch stable (`a3803f9`)
- Add rdflib dependency (`4cc1b96`)
- Add Generator and Sequence imports (`3369d44`)
- Add jaxtyping torch tensor annotation (`8d8d405`)
- Fetch s800classed from the stable branch (`3ebfde7`)
- Get s800classed from git (`36a8edc`)
- Use https to fetch s800classed.git (`313cb4f`)
- Allow gpu or cpu builds (`6ecc1cd`)
- Cpu portability (`e797047`)
- Requiring Python <3.12 for torch.dynamo (`55f51c5`)
- Downgrade back to torch stable (`08d6168`)
- Add rdflib dependency (`c817be2`)
- Add Generator and Sequence imports (`538a537`)
- Add jaxtyping torch tensor annotation (`417896e`)
- Clean up unused dependencies (`053ffc1`)
- Torch dynamo now supports Python 3.12 (`6a374de`)
- Clean up pyproject.toml (`417327b`)
- Add pydantic, fastapi and sqlmodel dependencies (`fc876b8`)
- README.md (`cb44a9f`)
- Cleanup (`c218693`)
- Document the ExponentialLR argument (`c0a2cd0`)
- Allow explicit selection of gpu with --with gpu (`1dda9ee`)
- Cleanup (`a7b5152`)
- Remove obsolete tests (`0679504`)
- Cleanup (`0973b1b`)
- Cleanup (`3c8b89c`)
- Use "labels" instead of "tags" for NERC annotations (`a1e432e`)
- Cleanup (`c4a1e5c`)
- Config -> model_config (`cd490c0`)
- Cleanup (`6924717`)
- Cleanup (`4e7b83c`)
- Update tests so the cases are valid xml and closing tags are properly handled (`8b665d5`)
- Use double quotes for attribute values in the xml tests (`6d63005`)
- Imports cleanup (`23b983f`)
- Add test_div_with_attribs (`6f0d9d9`)
- Cleanup (`4a94399`)
- Cleanup (`4271389`)
- Update test_merged_prediction (`4cc5979`)
- Add some debugging info when parsing a response (`cbfde2a`)
- Cleanup (`874877a`)
- Cleanup (`045456f`)
- Cleanup and formatting (`31533d9`)
- Alea (`9e9add7`)
- Formatting (`5f045ce`)
- Cleanup (`74dc16b`)
- Remove extra div (`1d587a3`)
- Cleanup (`3661050`)
- Test_reinsert_tags_preserves_outer_div (`f3b7fc6`)
- Remove unused function get_chunk (`ffcc2f5`)
- Add tests for the hoisting of CURIEs to the top of the tree (`9c8fb33`)
- Cleanup (`566bc89`)
- Add test_merge_does_not_duplicate (`b566196`)
- Useful debugging functions (`7b89161`)
- Add '[CLS]' tokens to the test_merged_preditions samples (`95a49d9`)
- Add merge_predictions docstring (`50d413b`)
- Tomllib requires python 3.11 (`c303451`)
- Cleaning up unnecessary dependency specifications (`4f7f1a5`)
- Add rapidfuzz dependency (`b32b971`)
- Add strains_list to config (`2d80e13`)
- Local build (`799fbf9`)
- Add test_annotation_pipeline.py (`b54bd7a`)
- Use remote for s800classed (`d80c1c2`)
- Cleanup (`74e54f0`)
- Add docstrings (`e2969d8`)
- Migrate from poetry to uv (`a12e455`)
- Bump xmltodict version (`a0e629e`)
- Rename project to d3text (`69c596a`)
- Change file mode of feed.py (`dab6f24`)
- Change file mode of feed.py (`80e4861`)
- Add rocm package index (`dbfbc9a`)
- Declare jaxtyping and brenda-references dependencies (`12651a5`)
- Export brenda_dataset (`f4ea4f4`)
- Add docstring to BrendaDataset (`5627a50`)
- Document BrendaDataset.__getitem__ (`e285241`)
- .pre-commit-config.yaml (`7e8f1d4`)
- Add cu118 for the VM (`d9da854`)
- Model performances (`2e5407a`)
- Remove main.py (`7c5b78b`)
- Brenda_conn.toml (`d94ccde`)
- .pre-commit-config.yaml (`2102913`)
- Add cu118 for the VM (`2d8a1b1`)
- Remove unused dependencies (`a8090df`)
- Cleanup (`dd602d2`)
- Update test config (`c583549`)
- Update test config (`df32c7a`)
- Gitignore experiment config files (`f913bd4`)
- Remove old tuning.py (`adc6f62`)
- Type hint fixes (`1236ecc`)
- Clean-up (`b6e77c5`)
- Remove unused sqlmodel dep (`764b058`)
- Cleanup (`5c1580f`)
- Add docstrings and type hints (`57aef6b`)
- Setup mkdocs (`5ef941a`)
- Add docstrings (`9d40d6c`)
- Test_all_entity_classes_in_splits (`4c12ab5`)
- Add test_all_entity_classes_in_splits (`2c2e9d3`)
- Require python >=3.12 (`21157ad`)
- Correct ETEBrendaModel.ground_truth docstring (`5da5b2b`)
- Add beartype and pytest dev deps, and brenda-references (`419cd9e`)
- Fix imports in test_annotation_pipeline.py (`028086c`)
- Add brenda_references as submodule (`8473d92`)
- Fix imports in test_annotation_pipeline.py (`ce593eb`)
- Add ipython-autotime dev dependency (`3400f7e`)
- Add pudb dev dependency (`4c22a9f`)
- Update brenda_references (`d7c56c9`)
- Update brenda_references (`deac9a1`)
- Comments (`bb42377`)
- Bump rapidfuzz version (`262bce1`)
- Restrict to Python <3.13 (`b6f6215`)
- Add project metadata to pyproject.toml (`0b150b6`)
- Update brenda_references and pin xmlparser version (`5e3a2c5`)

### data

- Esummaries.json (`8f42d96`)

### deps

- Random, urllib (`04d7907`)

### dev

- Pytest and ipython to dev dependencies (`effa0de`)
- Remove flake and ruff deps (`e2185d1`)
- Tools to assess hyperparameter tuning (`2fbdf5f`)
- Remove flake8 config and set ruff line-length to 80 (`c8eb8ac`)
- Reducing the hyperparameter space (`34c8c73`)
- Reduce hyperparameter space (`ce5f003`)
- Reduce patience for early stopping and learning rate schedule (`c9e993e`)
- Keep layer normalization (`200887c`)
- Keep layer normalization (`ef56481`)
- Mypy ignores (`0e51b10`)
- Add logger to the backend (`9434d85`)
- Remove mccabe, superseded by ruff (`924c13d`)
- Make mypy quiet down about the table parameter on SQLModel (`f4ca9ac`)
- Removing memestra dep (`17b3c54`)
- Add watchfiles dependency (`4a5789d`)

### doc

- Clarify the scope of the comment (`d382e8b`)

### style

- Add indentation to data/esummaries.json (`855d68c`)
- This is INFO, not DEBUG (`9816f33`)
- Self._model -> self.base_model (`fd373d2`)
- Load -> load_dataset (`0eef21e`)
- Formatting (`a226482`)
- Formatting (`859d18a`)
- Formatting (`5571a6c`)
- Formatting (`b52e6b9`)
- Formatting (`d35fed3`)
- Formatting (`ea12a41`)
- Formatting (`291436b`)
- Formatting (`668fa0a`)
- Formatting (`69309c6`)
- Formatting (`550f20b`)
- Formatting (`c464219`)
- Sort dependencies (`89eaf19`)
- Formatting (`6d4b198`)
- Formatting (`2ab21ef`)
- Formatting (`2764c70`)
- Formatting (`abaeb81`)
- Clean up (`1979b7b`)

