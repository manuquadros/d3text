# Changelog

Every released version of brenda_references, newest first, generated by
`pdm run changelog` from the commit history. Do not edit by hand.

## 0.2.0 — 2026-09-16

### Added

- Collect statistics by entity type and plot reference counts (`c882b7a`)
- Clean_data.py script (`db7f4d5`)
- Count related strains (`aaad88f`)
- Distribute the BRENDA data blobs via the Hugging Face Hub (`71d1e6e`)
- Add a synchronous entry point for sync_doc_db (`0ebcfbb`)
- Wire the enzyme-negative pool into train/val/test (`67c37a8`)

### Fixed

- Seed and partition the noise pool so splits stay disjoint (`481c8d0`)
- Ship brenda_references' config.toml, take the DB host from env (`569bbac`)
- Write regenerated splits where the package reads them (`18dcdce`)
- Make beartype optional in brenda_references' scripts (`4e43c48`)
- Drop the dead console-script entry points from brenda_references (`261e849`)
- Repoint brenda_references/scripts/ imports at their new homes (`05c545f`)
- Refuse a negative --limit instead of emptying the split (`eeb7b19`)
- Reject dataset_splits shares that would go negative (`1e3c03b`)
- Compare other_bacids against str ids by coercion (`012cefc`)
- Skip malformed records instead of crashing docdb lookups (`b3bc762`)
- Remove unreachable Git LFS filter declarations (`e692ec0`)
- Return an empty split when dataset_splits drains its pool (`d1c2622`)
- Order the relation none-fill's pairs like the typed keys (`596252f`)
- Await store_strains so sync_doc_db actually stores strains (`758411f`)
- Log organism names fix_taxonomy cannot decompose (`abf652d`)
- Refuse to open a missing BRENDA doc database (`396d481`)
- Reject unrecognized BrendaDocDB storage values (`b077487`)
- Exclude the psycholinguistics pool's enzyme-naming rows (`df90b5d`)
- Refuse a StrainInfo join whose designation isn't a registry id (`76ef50d`)

### Changed

- Center the title of the frequency distribution plot (`a2393ec`)
- Parameterize the limits of the plot (`4708b76`)
- Publication version (`aae21f2`)
- Bring brenda_references back under ruff format (`b1bfaa7`)
- Make docdb.py's Table.get typing and errors honest (`55d1b54`)

### Internal

- Remove sources, leave it to the workspace (`5586cfb`)
- Beartype the scripts (`0062d90`)
- Alea (`2db2346`)
- Add brenda_references as subtree (`9c0fa2d`)
- Source local sub-packages from git; fix brenda_references deps (`64f83a9`)
- Add huggingface_hub dep to brenda_references (`676b25d`)
- Drop uv and install through pdm alone (`0a6dd16`)
- Mark test_expand_doc_gets_pmc_open as integration (`01cf884`)
- Drop two dead-code ruff findings from prior review (`ebf9a4a`)
- Move design rationale out of docstrings into the doc site (`4a0dd4d`)
- Pin enzyme_negative_pool.json in the data manifest (`b40a986`)
- Re-pin enzyme_negative_pool.json after upstream fixes (`e33a5db`)
- Add release infra (git-cliff + scm versioning) (`c1282d0`)
- Generate CHANGELOG.md with git-cliff (`a4ab604`)

### ci

- Give brenda_references its own test workflow (`99190ec`)

### merge

- Pull in d3text-local brenda_references history (`2f16756`)

## 0.1.0 — 2025-10-06

### Added

- Add __main__.py (`c94bf2c`)
- Add sync_doc_db (`34ce278`)
- Ncbi module to retrieve records from NCBI (`8071d07`)
- Module log for logging (`29aaeb1`)
- Track created and modified time of each document record (`e8294d2`)
- Retrieve pmc_id, doi and full-text access information (`8797a75`)
- Retrieve EC class synonyms (`0fe14d9`)
- Add lspn synonyms lookup (`7f9cac8`)
- Resolve strain synonyms (`5084735`)
- Retrieve straininfo ids before retrieving strain data (`3c15ee3`)
- Add all synonyms to the document model (`9de5770`)
- Db.brenda_bacterial_references (`20668c1`)
- Brenda_enzyme_relations to retrieve relations for a reference_id (`a946836`)
- Add year field for documents (`c4e772f`)
- Validate Strain.taxon recording cases of missing info (`19b0ad0`)
- Normalize_strain_names (`6bcc279`)
- Generalize normalize_strain_names to make more substitutions (`fd8e88a`)
- Retrieve_abstracts script (`a265a91`)
- Fetch_ncbi_abstracts (`f799c6c`)
- Add_abstracts (`986bfa6`)
- Fix_missing_strains (`e53bc68`)
- Preannotate script (`12e7c28`)
- Add comparison operators to EntityMarkup (`26bb725`)
- Scripts/generate_json_schemas.py (`bf3dff8`)
- Add stderr_logger (`491288e`)
- Generate_entity_names_dataset (`5d017bd`)
- Add stderr_logger (`a15c1ad`)
- Generate_entity_names_dataset (`103e98f`)
- Add check for scanned articles from NCBI (`5aa2173`)
- Present statistics about full text availability (`5c78e07`)
- Add the brenda_references.queries modules (`f808c2a`)
- Sampling.py (`5feb6bc`)
- Add resources/ (`5c5cb9c`)
- Add stub for documents function with type hint (`efba295`)
- Documents() query (`40a2724`)
- Lpsn_parent (`fe482e8`)
- Add docdb module to brenda_references package (`f060855`)
- Strain_by_designation (`651a953`)
- Script used to create the test document database (`d506026`)
- Update_record method (`7c3d7c9`)
- Scripts/fix_taxonomy.py (`8675366`)
- Add_synonyms, generalizinv over bacteria and strains (`8b11c48`)
- Scripts/fix_pubmed_ids (`f60900b`)
- Add GMESampler class (`3e271a6`)
- Add GMESampler.dataset_splits (`25e148b`)
- Scripts/generate_dataset.py using GMESampler (`2ccda8c`)
- Dataset module with functions for loading the data splits (`e6122b0`)
- Add preprocess_labels (`922450b`)
- Add psycholinguistics_data (`12ee7dd`)
- Limit parameter on dataset loading (`074d5eb`)
- One-hot encode relations (`2bd4d0b`)
- Entities_in_dataset and jaccard_similarity (`8a3e01f`)
- Add data files (`717d571`)
- Augment_training_data (`a5d809b`)
- Add limit parameter to load_split (`c25ff33`)

### Fixed

- Compute the right path of config.toml (`5231fc5`)
- Base _Reference on BaseReference (`05be4d2`)
- Add None value as default for the fields not in the db (`89e8b75`)
- Handle articles without a pubmed_id (`9411376`)
- Set default pmc_open as False (`3db6820`)
- Serialize Pydantic models with mode="json" (`8e42bb7`)
- Validate when loading documents from the document database (`ece8ec2`)
- Use the correct _Strain.name for name cleaning (`c44abaf`)
- Make sure the sequence of ids is hashable and order-invariant (`a53a4b6`)
- Replace frozenset with set in the models (`5893863`)
- Make sure _Strain is hashable (`8c84c6c`)
- Return tuple from get_strain_data instead of generator (for caching) (`4889dac`)
- Remove "pv." (pathovar) for matching with lpsn (`cd6a12d`)
- Remove ssp. before matching to lpsn (`e618dd4`)
- Handle Timeout exceptions (`7ee86e5`)
- Corretly detect when the full text of an article is available on PMC (`11c2ba9`)
- Clear buffer after updating the JSON db (`94f8329`)
- Handle bacteria name from brenda that already contain a strain designation (`ef0c0fa`)
- Use column_property to map protein_organism_strain_id to Strain.id (`4136b44`)
- Use sa_column_kwargs to define the SQLAlchemy alias and freeze the model (`6809a3c`)
- Handle PubMed IDs incorrectly containing non-numerical characters in BRENDA (`28ef343`)
- Handle bacteria name from brenda that already contain a strain designation (`7139e80`)
- Add the triples found in each reference (`76d6ef7`)
- Use a list because set is not JSON serializable (`1a4dbbe`)
- Refer to the correct type: datetime.datetime (`32ef82f`)
- Make sure sets are properly serialized (`7097d59`)
- Make expand_doc visible; fix typos (`3b90424`)
- Map strain mentions in BRENDA to their Straininfo entries (`5859be0`)
- Update documents correctly (`4ccf5ba`)
- Make bacteria and other_organisms dict[int, str] (`c4e275c`)
- Allow for a single str argument in normalize_strain_names (`4d88547`)
- Transform a _Strain model into a single Strain with normalized designations (`0ebe7f0`)
- Accept str args and do not worry about normalization in get_strain_ids (`60688db`)
- Add StrainRef, to bridge between the db tables and pydantic (`d7ea7f9`)
- Index indexed_buffer with int (`8dd3d70`)
- Make fetch_ncbi_abstracts safer - check whether abstract.text exists (`16b274d`)
- Reponse -> response, remove unused imports, replace requests with httpx (`e2f02a0`)
- Add context management dunders to APIAdapter (`706f282`)
- Add tqdm_asyncio and NCBIAdapter imports to preannotate.py (`eac26d8`)
- Use asyncio compatible middlewares for TinyDB (`e4cbe7c`)
- Add missing imports (`4fde0f2`)
- Check for empty abstracts in preannotate.py (`7ddc7c7`)
- Refer to docdb and to doc_id (`76004ca`)
- Solve FutureWarning agains "if abstract:" (`49b25df`)
- Add missing arguments to fetch_and_annotate_call (`218e8dc`)
- Add missing imports (`7397745`)
- Remove await from non-awaitable function call (`2ace359`)
- Rate limit requests in APIAdapter (`95e33b2`)
- Asyncify APIAdapter (`b0552d2`)
- Httpx.exceptions.InvalidURL -> httpx.InvalidURL (`e4c1c4f`)
- Check length of the text attribute of the Element, not of the Element itself (`9583aab`)
- Return immediately when abstract *is* empty in mark_entities (`46192b7`)
- Do not await explicitly for db updates in preannotate (`8e0eb51`)
- Update test_annotation with asyncio (`8592ffb`)
- Eliminate the use of gather in the preannote async loop (`6bce63d`)
- Set the correct number of iterations for the progress bar (`c5f60eb`)
- Only process documents that have not been reviewed after creation (`7ffe987`)
- Properly count the number of processed abstracts per batch (`e27b2e4`)
- Mark doc as reviewed even if nothing changed (`3dc20c6`)
- Filter for reviewed date outside of the TinyDB query (`dcbbd19`)
- Clean up pubmed_id and pmc_id when validating Document (`30d02da`)
- Remove all invalid characters from BRENDA pubmed ids (`019b455`)
- Call mark_entities instead of fetch_and_annotate for tests with abstract (`8bc8577`)
- Mark doc as reviewed even if nothing changed (`340532d`)
- Filter for reviewed date outside of the TinyDB query (`dc46b95`)
- Clean up pubmed_id and pmc_id when validating Document (`03a4a50`)
- Remove all invalid characters from BRENDA pubmed ids (`35e01c8`)
- Remove incomplete line in retrieve_text.py script (`ca729e9`)
- Add fulltext field to the Document model (`f9fd915`)
- Update loggers and apiadapters imports (`bfd78bd`)
- Ncbi adapter import (`737b88b`)
- Check if query is an Integral to include numpy.int64, for example (`2cf44ee`)
- Remove "serovar" from the name parts as well (`464403c`)
- Do not remove "sp." from species names (`8edcd83`)
- Correctly specify the type of storage for BrendaDocDB (`1c006bb`)
- Solve mypy complaints about the return type of table.get (`a4f8c6b`)
- Use the new interface to call apiadapters.ncbi (`9bedaf0`)
- Handle cases where there is no there is no LPSN id for a term (`07f5b90`)
- Make BrendaDocDB.__add_bacteria_record thread-safe (`229fa36`)
- Make sure sets are turned to lists prior to serialization (`3cf727a`)
- Several import fixes (`ddff37f`)
- Include serotype in the list of name_parts replacements (`186d311`)
- Rename variables so as not to redefine `object` (`c75de52`)
- Add stderr_logger import (`bad2861`)
- NCBIAdapter -> AsyncNCBIAdapter (`e169e6c`)
- File_logger is not callable (`e428a82`)
- Sample only documents containing a pubmed_id (`135c529`)
- Correct DATA_DIR path (`66d83cf`)
- Reflecting changes in the ncbitax interface (`986ab9f`)
- Test expand_doc asynchronously (`25d777a`)
- Filter out bacteria documents without strain-level annotation (`0d9b851`)
- Filter out docs with strains and no bacteria (`3931be0`)
- Filter out documents with bacteria and no strain annotations (`02a2af5`)
- Import from d3types instead of brenda_types (`e297d41`)
- Remove module refs (`f8d0071`)

### Changed

- Add documents and entities to config.toml (`d6c8fb9`)
- DocumentStore class (`ca4165e`)
- Add config.py (`4ef55ab`)
- Bacteria and Strain type aliases (`57d9f5e`)
- Collect the strain name registered in brenda directly (`589d032`)
- Add checks for invalid pubmed ids (`775cc17`)
- Choose straininfo api_function depending on argument type (`aa69ed5`)
- Save store to config["documents"] (`7c18092`)
- Get_document out of sync_doc_db (`83b348f`)
- Retrieve EC synonyms with one db query only (`19556cf`)
- Cache the ec_synonyms query (`f880d2c`)
- Use tinydb for the document database (`d5211e4`)
- Cache functions that make API requests (`f886430`)
- Retry api calls if they fail with too many requests (`42a602a`)
- Ignore init_vars.sh and data/documents.json (`3d0dfa0`)
- Be explicit about the use of straininfo ids (`bc8e3ef`)
- Remove sqlmodel echo (`a08404b`)
- Return an iterator from get_strain_data (`a167da7`)
- Wait at least 1s between api calls (`2b3d464`)
- Frozenset in some models (`a0dc533`)
- NCBIAdapter class to manage the NCBI connection (`af09881`)
- Ignore BRENDA references that are in the JSON database already (`22e3691`)
- APIAdapter with subclasses NCBIAdapter and StrainInfoAdapter (`3da19e3`)
- Remove unused imports (`3efe7fd`)
- Alea (`e5ac0a1`)
- Add sync_doc_db as a Poetry script (`ebe670f`)
- Rename __main__.py files to more descriptive names (`d8eae6a`)
- Remove the check for strains only mentioned in the organism name (`eab77d2`)
- Replace known_designation with an id check on the strains table (`a2ceaea`)
- Remove stopgap solution to missing relations (`ad6961d`)
- Identify strains by id in Document (`9f1f563`)
- Return Strain models from brenda_enzyme_relations (`4da94c8`)
- Rename "id" as "siid" when retrieving straininfo data (`e532292`)
- Use BRENDA ids for the strains, adding Straininfo ids when available (`6e6c622`)
- Class BRENDA to manage the database session (`ae73c90`)
- Bacteria_synonyms -> store_bacteria (`edace04`)
- Remove unreachable code (`61d927b`)
- Simplify enzyme_synonyms, renamed as store_enzyme_synonyms (`4fd156c`)
- Store only enzyme ids in the Document model (`155796e`)
- Remove return value from add_document (`0298038`)
- Rename store_strains parameter (`bd8a1ec`)
- Remove return value from add_document (`72ff396`)
- Remove `last_call` and `maybe_wait` dead code (`0d0a2b7`)
- Add the abstract field to Document (`6238a90`)
- Increase timeout a bit (`9625b57`)
- Feed StrainRef arguments explicitly (`26b1e1c`)
- Factor retrieve_strain_models out of __flush_buffer (`78c0f44`)
- Extract info from the XML tree instead of converting it to a dict (`de5ae7c`)
- Preserve formatting tags inside the AbstractText element (`b0fd055`)
- Include all documents when retrieving abstracts (`8e02985`)
- Using aiotinydb and httpx with asyncio (`884e7a5`)
- Remove reference to tinydb (`af96dc4`)
- Add checks to make sure entities have been retrieved from the db (`0cbfa35`)
- Add checks to catch db retrieval problems (`1e5a7a1`)
- Let exceptions propagate (`c9472f7`)
- Do not update the database within fetch_and_annotate (`6c84ad2`)
- Remove pprint (`22480f2`)
- Add check for empty text in fuzzy_find_all (`8db9624`)
- Add_abstracts in brenda_references (`4ecaeb0`)
- Remove unnecessary compreension (`f771c60`)
- Remove unnecesary len call (`2a13428`)
- Concurrently process three batches each time (`3c6f6ca`)
- Process only documents without entity span annotations (`e6b35c5`)
- Exit when there is no document to annotate (`c8f98e4`)
- Remove dead code: src/db.py (`65ba477`)
- Remove unnecessary exception check (`6c4b8cf`)
- Use more standard exception handling (`fbdc7a5`)
- Fix redefined-loop-name (PLW2901) (`b5aa909`)
- Cover the edge case of an empty `pattern` (`34021a0`)
- Rename "modified" to "reviewed" (`bf352c7`)
- Asyncify mark_entities (`2f56cee`)
- Remove unnecessary else (`e033b56`)
- Remove validate_taxon, not really necessary (`49e1783`)
- Remove unnecessary call to doc.update (`38e311d`)
- Remove serialize_in_order, inline sorted instead (`2ace129`)
- Remove record_nos (dead code) (`f6f62fb`)
- Move ration, fuzzy_find_all, and abbreviate_bacteria to utils.py (`4480c10`)
- Add some logging information to catch malformed ids in add_abstract (`4dd0a73`)
- Asyncify mark_entities (`2dba662`)
- Remove unnecessary else (`daee0fa`)
- Remove validate_taxon, not really necessary (`ab9d62f`)
- Remove unnecessary call to doc.update (`0887bd0`)
- Remove serialize_in_order, inline sorted instead (`6584d98`)
- Remove record_nos (dead code) (`6048d38`)
- Move ration, fuzzy_find_all, and abbreviate_bacteria to utils.py (`0fbeef6`)
- Add some logging information to catch malformed ids in add_abstract (`f8014ad`)
- Move scripts folder to project root (`ce6ea98`)
- Multiple changes - or a bit of a mess (`d4a1a8f`)
- Migrate retrieve_text.py to async with AIOTinyDB and improved error handling (`0b4f2c7`)
- Remove unnecessary attribute check in async setitem method (`3f9ae03`)
- Modify document retrieval and flushing logic in retrieve_text script (`3242d87`)
- Optimize async text retrieval with semaphore and improved error handling (`4c7b6e0`)
- Simplify scripts/retrieve_text (`f564258`)
- Delete log and ncbi (moved to their own repositories) (`3e37a9b`)
- Change project name and email on pyproject.toml (`a84c8d6`)
- Delete log and ncbi (moved to their own repositories) (`bd72371`)
- Remove tests/__init__.py (`c29edaa`)
- Add data folder (`418ebc3`)
- Rename sampling test_file (`47aa967`)
- Search_bac_by_name -> bacteria_by_name to make query names more consistent (`9280dd8`)
- Use stderr_logger (`89b0535`)
- Use a helper function inside add_bac_synonyms and make mypy happy (`9f5f930`)
- Insert_bacteria_record, instead of get_or_add_bacteria_record (`c775bec`)
- Handle insertion of already existing docs gracefully (`aad84fb`)
- Update import statements and add StrainInfoAdapter import (`175b06b`)
- Move brenda_types and lpsn_interface to their own package directories (`39180d4`)
- Return the doc_id of the new record on BrendaDocDB.insert (`cda8de3`)
- Factor bacteria and strain updates out of fix_taxonomy (`67bb148`)
- Factor out reference_counts (`4f5689b`)
- Log to file instead of stderr in lpsn_interface.py (`6e83407`)
- Filter the set of references before processing them (`fdb10c7`)
- Only update the other_organisms field when necessary (`ba21d6b`)
- Rename ncbitax.decompose_name (`b80d349`)
- Add types to the sampling dataframe (`c224363`)
- Add types to the sampling dataframe (`bf4f353`)
- Used the decomposed name to update the bacteria and strain fields (`c6b9164`)
- Rename build_df -> build_sampling_df (`e973b86`)
- Turn item_column and on_columns into class parameters (`6afa433`)
- Add loading message (`d5e32a4`)
- Move loading functions to brenda_references.py (`32219fb`)
- Test synchronously (`e34009d`)
- Set empty default for Document.synonyms (`e67583f`)
- Remove tags from psycholinguistics abstracts (`028b767`)
- Remove the limit parameter (`b79e53a`)
- Move filtering to the dataset generation step (`b97f4bf`)
- First compute GME sampling on the training data (`853a781`)
- Move brenda_types to d3types (`15b3653`)
- Move lpsn_interface to apiadapters (`00d7e8d`)
- Use importlib to resolve configs (`0b80a2b`)
- Move data files into the src tree (`4417121`)
- Ignore big files (`6845072`)
- Remove unused dependency (`2ee3643`)
- Specifiy ents type in merge_entcols (`3ad0699`)
- Remove loggers dependency (`245ee17`)

### Internal

- Add tqdm dependency (`a3c92e5`)
- Add requests and xmltodict dependencies (`072e302`)
- Add test_apis.py (`4c6bae5`)
- Pytest-memray dev dependency (`2707105`)
- Remove unnecessary mysqlclient dependency (`016d2b2`)
- Rich as dev dependency (`9f5ffcc`)
- Remove unused imports (`7e23756`)
- Stackprinter dev dependency (`8aebd43`)
- Remove unused imports (`5e50b71`)
- Docstrings (`dbeddce`)
- Docstrings (`b47ac73`)
- Add ROOT_DIR constant to config.py (`cb17095`)
- Add Sphinx documentation (`e9195ab`)
- Docstrings (`23cb435`)
- Add mysqlclient dependency (`22c91ef`)
- APIAdapter docstring (`073db7b`)
- Update dependencies (`7b23b53`)
- Add .pylintrc with lxml in the extension-pkg-allow-list (`9411a54`)
- Update tests to use asyncio (`ca0ec14`)
- Add return type annotation to log.logger (`ce70eef`)
- Remove unused imports (`2d480e8`)
- Add module docstring to generate_json_schemas.py (`ccb29f2`)
- Reflect declarative_base move in the sqlalchemy API (`c3119c8`)
- Formatting, cleanup (`cf64922`)
- Migrate to uv (`b18241b`)
- Add src/scripts to the distribution (`06938b2`)
- Fix import but unused errors (`d63096a`)
- Fix E711 (`0af3c9c`)
- Sort imports (`9c0dd0a`)
- Formatting (`8875134`)
- Fix pyupgrade linting errors (`08ddada`)
- Add type annotations (`e18dd3a`)
- Improbe add_abstracts' docstring (`9fab33d`)
- Stubs and linting rules (`2a55737`)
- Fix import but unused errors (`f888739`)
- Implement __hash__ for Triple and EntityMarkup (`24e2c3b`)
- Add missing return type annotations (`da79da2`)
- Clean up (`c71796a`)
- Add some docstrings (`bda0557`)
- Add test_deak (`2334113`)
- Fix import but unused errors (`b599b4d`)
- Implement __hash__ for Triple and EntityMarkup (`6e3db51`)
- Add missing return type annotations (`59ee3ae`)
- Clean up (`d85e940`)
- Add some docstrings (`1bb4a77`)
- Add test_deak (`763262a`)
- Update LPSN data (`fd6e9c3`)
- Add gme and apiadapter deps (`3278efc`)
- Sort dependencies and remove prospector (`245c5a2`)
- Clean up pyproject.toml (`b7bec88`)
- Leave build target file inclusion for the VCS, add pytest dev dependency (`fa747d3`)
- Add .coverage to .gitignore (`b10946c`)
- Fix pubmed_id in test_sampling.py (`00c5f62`)
- Add pytest dev dependency and configure coverage reporting (`9b7282c`)
- Remove queries.py, replaced by docdb.py (`24a447a`)
- Clean __init__.py: remove icecream (`fdfa024`)
- Test_docdb.py and test_files/testdb.json (`cb7540c`)
- Add ncbitax dependency (`acbc0b0`)
- Make sure scripts are visible to tests (`d9fe410`)
- Test_fix_testdb (`17d5a28`)
- Add misscategorized strain data to the test db (`255ea3d`)
- Test_fix_strains (`2887705`)
- Fix assertion of strain_id existence (`ecaafb5`)
- Use-implicit-booleaness-not-len (`a6c90fb`)
- No-else-return (`3da2e4b`)
- No-else-break (`19ed7be`)
- Remove implicit str-concat in function call (`fae0210`)
- Remove xmltodict dependency (`de81ad3`)
- Test_taxonomy.test_29345379 (`d43bfcd`)
- Test_taxonomy.test_29345379 (`0e93a85`)
- Cache test data loading (`09ae170`)
- Update the document in the new test (`8292767`)
- Update testdb.json (`9f61f48`)
- Update list of bacteria in test_29345379 (`af6e35c`)
- Fix imports (`7ebe372`)
- Export dataset splits (`b5d4d7b`)
- Removing irrelevant tests (`60842f0`)
- Add pytest-watcher dev dep (`3e89df4`)
- Test id_retrieval with set inclusion, allowing the API to chance (`04e6a37`)
- Remove unused dependencies (`b07e1f5`)
- Require python >=3.12 (`c5c8628`)
- Add pytest-cov dependency (`2ce0abb`)
- Add pytest-asyncio dev dep (`bae53fa`)
- Dev dependencies (`b86cbd1`)
- Training_data.csv to lfs (`b29e6c4`)
- Pin apiadapters version (`32268d3`)
- Add LICENSE (`8976663`)

### cleanup

- Remove brenda_bacterial_references and protein_connect_records (`aa51d73`)
- Remove BrendaStrain (`fee74e3`)

### dev

- Add flake8 (`cb86573`)
- Mccabe, devtools, pytest-sugar as dev dependencies (`eff5c42`)

### style

- Renamings (`454eb5d`)
- Reorder types for clarity (`f3c98c8`)
- Close the connection before returning (`a24f590`)
- Formatting (`4bba0be`)
- Multiple formatting  fixes (`a77619c`)
- Formatting (`2f89d86`)
- Formatting (`5135f70`)
- Formatting (`7aa5c59`)
- Sort imports (`d3d5b0b`)
- Sort imports (`b262566`)
- Sort imports (`45651cf`)
- Formatting (`993e36b`)
- Formatting (`bf679dd`)
- Formatting (`1ffe726`)

